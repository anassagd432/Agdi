# Next.js Environment Variables (Optional)
# The dashboard connects to the Agdi Gateway entirely client-side.
# However, if you plan to extend the dashboard with server-side logic,
# configure your environment here.

# NEXT_PUBLIC_AGDI_DEFAULT_GATEWAY=ws://localhost:18789
# NEXT_PUBLIC_AGDI_DEFAULT_TOKEN=your-token-here
