---
summary: "CLI reference for `agdi clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `agdi clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `agdi clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `agdi clawbot qr` (same behavior as [`agdi qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `agdi clawbot qr` -> `agdi qr`
