---
summary: "CLI reference for `agdi sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `agdi sessions`

List stored conversation sessions.

```bash
agdi sessions
agdi sessions --active 120
agdi sessions --json
```
