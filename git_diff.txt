commit 63de26800f2966c0b3841b2b295b275ce0681c44
Author: anassagd432 <anassagd33@gmail.com>
Date:   Fri Feb 27 03:38:50 2026 +0100

    feat(ui): add explicitly clickable UI buttons for active agent-mode features
    
    Adds Quick Action buttons for Canvas, Run Command, and Browse in chat view, and Spawn Sub-Agent button in Agents Overview panel.

diff --git a/lint-err.txt b/lint-err.txt
new file mode 100644
index 000000000..306b881d8
--- /dev/null
+++ b/lint-err.txt
@@ -0,0 +1,4196 @@
+
+> agdi@2026.2.16 lint C:\Users\anass\Documents\GitHub\Agdi
+> oxlint --type-aware
+
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/grid-overlay.ts:74:31]
+ 73 |       // Skip the very first cell (0,0) as it would overlap with content
+ 74 |       if (x === 0 && y === 0) continue;
+    :                               ^^^^^^^^^
+ 75 | 
+    `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/goal-queue.ts:139:62]
+ 138 |     this.goals = this.goals.filter((g) => {
+ 139 |       if (g.status !== "completed" && g.status !== "failed") return true;
+     :                                                              ^^^^^^^^^^^^
+ 140 |       return new Date(g.updatedAt).getTime() > cutoff;
+     `----
+  help: Replace `return true;` with `{return true;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/goal-queue.ts:143:22]
+ 142 |     const removed = before - this.goals.length;
+ 143 |     if (removed > 0) this.persist();
+     :                      ^^^^^^^^^^^^^^^
+ 144 |     return removed;
+     `----
+  help: Replace `this.persist();` with `{this.persist();}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/goal-queue.ts:175:32]
+ 174 |       const bActive = b.status === "pending" || b.status === "in-progress" ? 0 : 1;
+ 175 |       if (aActive !== bActive) return aActive - bActive;
+     :                                ^^^^^^^^^^^^^^^^^^^^^^^^^
+ 176 | 
+     `----
+  help: Replace `return aActive - bActive;` with `{return aActive - bActive;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/goal-queue.ts:180:22]
+ 179 |       const pb = PRIORITY_ORDER[b.priority] ?? 99;
+ 180 |       if (pa !== pb) return pa - pb;
+     :                      ^^^^^^^^^^^^^^^
+ 181 | 
+     `----
+  help: Replace `return pa - pb;` with `{return pa - pb;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:105:16]
+ 104 |     const rule = this.rules.get(ruleId);
+ 105 |     if (!rule) return false;
+     :                ^^^^^^^^^^^^^
+ 106 |     this.rules.delete(ruleId);
+     `----
+  help: Replace `return false;` with `{return false;}`.
+
+  x eslint(curly): Expected { after 'for-of'.
+     ,-[src/autonomous/file-watcher.ts:125:63]
+ 124 |   stopAll(): void {
+ 125 |     for (const watcher of Array.from(this.watchers.values())) watcher.close();
+     :                                                               ^^^^^^^^^^^^^^^^
+ 126 |     this.watchers.clear();
+     `----
+  help: Replace `watcher.close();` with `{watcher.close();}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:144:22]
+ 143 |     const watcher = watch(directory, { persistent: false }, (eventType, filename) => {
+ 144 |       if (!filename) return;
+     :                      ^^^^^^^
+ 145 |       void this.handleEvent(directory, eventType, filename);
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:182:58]
+ 181 |     for (const rule of Array.from(this.rules.values())) {
+ 182 |       if (rule.directory !== directory || !rule.enabled) continue;
+     :                                                          ^^^^^^^^^
+ 183 |       if (rule.event !== "any" && rule.event !== event.type) continue;
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:183:62]
+ 182 |       if (rule.directory !== directory || !rule.enabled) continue;
+ 183 |       if (rule.event !== "any" && rule.event !== event.type) continue;
+     :                                                              ^^^^^^^^^
+ 184 | 
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:186:62]
+ 185 |       // Extension filter
+ 186 |       if (rule.extensions && !rule.extensions.includes(ext)) continue;
+     :                                                              ^^^^^^^^^
+ 187 | 
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:191:55]
+ 190 |         const pattern = rule.pattern.replace("*", ".*");
+ 191 |         if (!new RegExp(pattern, "i").test(filename)) continue;
+     :                                                       ^^^^^^^^^
+ 192 |       }
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/file-watcher.ts:195:84]
+ 194 |       // Cooldown
+ 195 |       if (rule.lastTriggered && Date.now() - rule.lastTriggered < rule.cooldownMs) continue;
+     :                                                                                    ^^^^^^^^^
+ 196 | 
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(no-unused-vars): Identifier 'basename' is imported but never used.
+    ,-[src/autonomous/file-watcher.ts:13:25]
+ 12 | import { stat, readdir } from "node:fs/promises";
+ 13 | import { join, extname, basename } from "node:path";
+    :                         ^^^^|^^^
+    :                             `-- 'basename' is imported here
+ 14 | import type { NLCommander } from "./nl-commander.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-dupe-keys): Duplicate key 'AGDI_CONFIG_PATH'
+     ,-[scripts/zai-fallback-repro.ts:105:5]
+ 104 |     ...process.env,
+ 105 |     AGDI_CONFIG_PATH: configPath,
+     :     ^^^^^^^^|^^^^^^^
+     :             `-- Key is first defined here
+ 106 |     AGDI_STATE_DIR: stateDir,
+ 107 |     AGDI_CONFIG_PATH: configPath,
+     :     ^^^^^^^^|^^^^^^^
+     :             `-- and duplicated here
+ 108 |     AGDI_STATE_DIR: stateDir,
+     `----
+  help: Consider removing the duplicated key
+
+  x eslint(no-dupe-keys): Duplicate key 'AGDI_STATE_DIR'
+     ,-[scripts/zai-fallback-repro.ts:106:5]
+ 105 |     AGDI_CONFIG_PATH: configPath,
+ 106 |     AGDI_STATE_DIR: stateDir,
+     :     ^^^^^^^|^^^^^^
+     :            `-- Key is first defined here
+ 107 |     AGDI_CONFIG_PATH: configPath,
+ 108 |     AGDI_STATE_DIR: stateDir,
+     :     ^^^^^^^|^^^^^^
+     :            `-- and duplicated here
+ 109 |     ZAI_API_KEY: zaiKey,
+     `----
+  help: Consider removing the duplicated key
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/device-controller.ts:278:19]
+ 277 |       return result;
+ 278 |     } catch (err: any) {
+     :                   ^^^
+ 279 |       log.error(`vision matching failed: ${err.message}`);
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:38:32]
+ 37 |     case "device_click": {
+ 38 |       if (!action.coordinates) throw new Error("device_click requires coordinates");
+    :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 39 |       await sleep(humanDelay(30, 100));
+    `----
+  help: Replace `throw new Error("device_click requires coordinates");` with `{throw new Error("device_click requires coordinates");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:47:9]
+ 46 |       if (!action.imageTemplatePath)
+ 47 |         throw new Error("device_click_image requires imageTemplatePath");
+    :         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 48 |       await sleep(humanDelay(50, 150));
+    `----
+  help: Replace `throw new Error("device_click_image requires imageTemplatePath");` with `{throw new Error("device_click_image requires imageTemplatePath");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:62:32]
+ 61 |     case "device_double_click": {
+ 62 |       if (!action.coordinates) throw new Error("device_double_click requires coordinates");
+    :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 63 |       await sleep(humanDelay(30, 100));
+    `----
+  help: Replace `throw new Error("device_double_click requires coordinates");` with `{throw new Error("device_double_click requires coordinates");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:70:32]
+ 69 |     case "device_right_click": {
+ 70 |       if (!action.coordinates) throw new Error("device_right_click requires coordinates");
+    :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 71 |       await sleep(humanDelay(30, 100));
+    `----
+  help: Replace `throw new Error("device_right_click requires coordinates");` with `{throw new Error("device_right_click requires coordinates");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:78:25]
+ 77 |     case "device_type": {
+ 78 |       if (!action.text) throw new Error("device_type requires text");
+    :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 79 |       if (action.coordinates) {
+    `----
+  help: Replace `throw new Error("device_type requires text");` with `{throw new Error("device_type requires text");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device-actions.ts:89:24]
+ 88 |     case "device_press_key": {
+ 89 |       if (!action.key) throw new Error("device_press_key requires key");
+    :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 90 |       await sleep(humanDelay(50, 150));
+    `----
+  help: Replace `throw new Error("device_press_key requires key");` with `{throw new Error("device_press_key requires key");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device-actions.ts:123:28]
+ 122 |     case "device_open_app": {
+ 123 |       if (!action.appName) throw new Error("device_open_app requires appName");
+     :                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 124 |       await controller.openApp(action.appName);
+     `----
+  help: Replace `throw new Error("device_open_app requires appName");` with `{throw new Error("device_open_app requires appName");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device-actions.ts:130:29]
+ 129 |     case "device_open_file": {
+ 130 |       if (!action.filePath) throw new Error("device_open_file requires filePath");
+     :                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 131 |       await controller.openFile(action.filePath);
+     `----
+  help: Replace `throw new Error("device_open_file requires filePath");` with `{throw new Error("device_open_file requires filePath");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device-actions.ts:137:24]
+ 136 |     case "device_open_url": {
+ 137 |       if (!action.url) throw new Error("device_open_url requires url");
+     :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 138 |       await controller.openUrl(action.url);
+     `----
+  help: Replace `throw new Error("device_open_url requires url");` with `{throw new Error("device_open_url requires url");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device-actions.ts:144:32]
+ 143 |     case "device_focus_window": {
+ 144 |       if (!action.windowTitle) throw new Error("device_focus_window requires windowTitle");
+     :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 145 |       await controller.focusWindow(action.windowTitle);
+     `----
+  help: Replace `throw new Error("device_focus_window requires windowTitle");` with `{throw new Error("device_focus_window requires windowTitle");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/windows-backend.ts:349:20]
+ 348 |   async minimizeWindow(titleOrId?: string): Promise<void> {
+ 349 |     if (titleOrId) await this.focusWindow(titleOrId);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 350 |     await powershell(`${ADD_TYPE_INPUT}
+     `----
+  help: Replace `await this.focusWindow(titleOrId);` with `{await this.focusWindow(titleOrId);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/windows-backend.ts:357:20]
+ 356 |   async maximizeWindow(titleOrId?: string): Promise<void> {
+ 357 |     if (titleOrId) await this.focusWindow(titleOrId);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 358 |     await powershell(`${ADD_TYPE_INPUT}
+     `----
+  help: Replace `await this.focusWindow(titleOrId);` with `{await this.focusWindow(titleOrId);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/types.ts:49:57]
+ 48 |   const p = process.platform;
+ 49 |   if (p === "linux" || p === "darwin" || p === "win32") return p;
+    :                                                         ^^^^^^^^^
+ 50 |   throw new Error(`Unsupported platform: ${p}`);
+    `----
+  help: Replace `return p;` with `{return p;}`.
+
+  x eslint(no-unused-vars): Identifier 'writeFile' is imported but never used.
+    ,-[src/autonomous/device/setup-agent.ts:11:20]
+ 10 | import { execFile } from "node:child_process";
+ 11 | import { readFile, writeFile, mkdir, access, constants } from "node:fs/promises";
+    :                    ^^^^|^^^^
+    :                        `-- 'writeFile' is imported here
+ 12 | import { homedir, platform } from "node:os";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Identifier 'mkdir' is imported but never used.
+    ,-[src/autonomous/device/setup-agent.ts:11:31]
+ 10 | import { execFile } from "node:child_process";
+ 11 | import { readFile, writeFile, mkdir, access, constants } from "node:fs/promises";
+    :                               ^^|^^
+    :                                 `-- 'mkdir' is imported here
+ 12 | import { homedir, platform } from "node:os";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Identifier 'access' is imported but never used.
+    ,-[src/autonomous/device/setup-agent.ts:11:38]
+ 10 | import { execFile } from "node:child_process";
+ 11 | import { readFile, writeFile, mkdir, access, constants } from "node:fs/promises";
+    :                                      ^^^|^^
+    :                                         `-- 'access' is imported here
+ 12 | import { homedir, platform } from "node:os";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Identifier 'constants' is imported but never used.
+    ,-[src/autonomous/device/setup-agent.ts:11:46]
+ 10 | import { execFile } from "node:child_process";
+ 11 | import { readFile, writeFile, mkdir, access, constants } from "node:fs/promises";
+    :                                              ^^^^|^^^^
+    :                                                  `-- 'constants' is imported here
+ 12 | import { homedir, platform } from "node:os";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Variable 'log' is declared but never used. Unused variables should start with a '_'.
+    ,-[src/autonomous/device/setup-agent.ts:16:7]
+ 15 | 
+ 16 | const log = createSubsystemLogger("setup");
+    :       ^|^
+    :        `-- 'log' is declared here
+ 17 | 
+    `----
+  help: Consider removing this declaration.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/device/setup-agent.ts:119:38]
+ 118 |         stderr: (stderr ?? error?.message ?? "").trim(),
+ 119 |         exitCode: error ? ((error as any).code ?? 1) : 0,
+     :                                      ^^^
+ 120 |       });
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/macos-backend.ts:46:18]
+ 45 |     execFile(cmd, args, { timeout: 10_000 }, (error) => {
+ 46 |       if (error) reject(new Error(`${cmd} failed: ${error.message}`));
+    :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 47 |       else resolve();
+    `----
+  help: Replace `reject(new Error(`${cmd} failed: ${error.message}`));` with `{reject(new Error(`${cmd} failed: ${error.message}`));}`.
+
+  x eslint(curly): Expected { after 'else'.
+    ,-[src/autonomous/device/macos-backend.ts:47:12]
+ 46 |       if (error) reject(new Error(`${cmd} failed: ${error.message}`));
+ 47 |       else resolve();
+    :            ^^^^^^^^^^
+ 48 |     });
+    `----
+  help: Replace `resolve();` with `{resolve();}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/macos-backend.ts:285:20]
+ 284 |   async minimizeWindow(titleOrId?: string): Promise<void> {
+ 285 |     if (titleOrId) await this.focusWindow(titleOrId);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 286 |     await osascript(`
+     `----
+  help: Replace `await this.focusWindow(titleOrId);` with `{await this.focusWindow(titleOrId);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/macos-backend.ts:297:20]
+ 296 |   async maximizeWindow(titleOrId?: string): Promise<void> {
+ 297 |     if (titleOrId) await this.focusWindow(titleOrId);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 298 |     await osascript(`
+     `----
+  help: Replace `await this.focusWindow(titleOrId);` with `{await this.focusWindow(titleOrId);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/macos-backend.ts:309:20]
+ 308 |   async closeWindow(titleOrId?: string): Promise<void> {
+ 309 |     if (titleOrId) await this.focusWindow(titleOrId);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 310 |     await this.hotkey(["meta"], "w");
+     `----
+  help: Replace `await this.focusWindow(titleOrId);` with `{await this.focusWindow(titleOrId);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:180:32]
+ 179 |     const result = await run(`which ${cmd}`);
+ 180 |     if (result.exitCode === 0) return name;
+     :                                ^^^^^^^^^^^^
+ 181 |   }
+     `----
+  help: Replace `return name;` with `{return name;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:416:36]
+ 415 |     );
+ 416 |     if (exitCode !== 0 || !stdout) return null;
+     :                                    ^^^^^^^^^^^^
+ 417 |     const parts = stdout.trim().split(/\s+/);
+     `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:447:15]
+ 446 |     const cmd = cmds[pm];
+ 447 |     if (!cmd) return { success: false, output: `Unknown package manager: ${pm}` };
+     :               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 448 | 
+     `----
+  help: Replace `return { success: false, output: `Unknown package manager: ${pm}` };` with `{return { success: false, output: `Unknown package manager: ${pm}` };}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:468:15]
+ 467 |     const cmd = cmds[pm];
+ 468 |     if (!cmd) return { success: false, output: `Unknown package manager: ${pm}` };
+     :               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 469 | 
+     `----
+  help: Replace `return { success: false, output: `Unknown package manager: ${pm}` };` with `{return { success: false, output: `Unknown package manager: ${pm}` };}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:489:15]
+ 488 |     const cmd = cmds[pm];
+ 489 |     if (!cmd) return { success: false, output: `Unknown package manager: ${pm}` };
+     :               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 490 | 
+     `----
+  help: Replace `return { success: false, output: `Unknown package manager: ${pm}` };` with `{return { success: false, output: `Unknown package manager: ${pm}` };}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:509:15]
+ 508 |     const cmd = cmds[pm];
+ 509 |     if (!cmd) return [];
+     :               ^^^^^^^^^^
+ 510 | 
+     `----
+  help: Replace `return [];` with `{return [];}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:529:15]
+ 528 |     const cmd = cmds[pm];
+ 529 |     if (!cmd) return false;
+     :               ^^^^^^^^^^^^^
+ 530 | 
+     `----
+  help: Replace `return false;` with `{return false;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:547:19]
+ 546 |       const [k, v] = line.split("=");
+ 547 |       if (k && v) props[k] = v;
+     :                   ^^^^^^^^^^^^^
+ 548 |     }
+     `----
+  help: Replace `props[k] = v;` with `{props[k] = v;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:799:19]
+ 798 |       const { stdout } = await run("cat /etc/os-release | grep PRETTY_NAME | cut -d'\"' -f2");
+ 799 |       if (stdout) distro = stdout;
+     :                   ^^^^^^^^^^^^^^^^
+ 800 |     } catch {
+     `----
+  help: Replace `distro = stdout;` with `{distro = stdout;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/linux-system.ts:879:25]
+ 878 |     const { stdout, exitCode } = await run("crontab -l 2>/dev/null");
+ 879 |     if (exitCode !== 0) return [];
+     :                         ^^^^^^^^^^
+ 880 |     return stdout
+     `----
+  help: Replace `return [];` with `{return [];}`.
+
+  x eslint(no-unused-vars): Identifier 'execFile' is imported but never used.
+    ,-[src/autonomous/device/linux-system.ts:22:26]
+ 21 | 
+ 22 | import { exec as execCb, execFile, spawn } from "node:child_process";
+    :                          ^^^^|^^^
+    :                              `-- 'execFile' is imported here
+ 23 | import {
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Identifier 'chown' is imported but never used.
+    ,-[src/autonomous/device/linux-system.ts:33:3]
+ 32 |   chmod,
+ 33 |   chown,
+    :   ^^|^^
+    :     `-- 'chown' is imported here
+ 34 |   access,
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Identifier 'basename' is imported but never used.
+    ,-[src/autonomous/device/linux-system.ts:49:34]
+ 48 | } from "node:os";
+ 49 | import { join, resolve, dirname, basename } from "node:path";
+    :                                  ^^^^|^^^
+    :                                      `-- 'basename' is imported here
+ 50 | import { promisify } from "node:util";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Variable 'exitCode' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/device/linux-system.ts:706:21]
+ 705 |   async clipboardPaste(): Promise<string> {
+ 706 |     const { stdout, exitCode } = await run(
+     :                     ^^^^|^^^
+     :                         `-- 'exitCode' is declared here
+ 707 |       "xclip -selection clipboard -o 2>/dev/null || xsel --clipboard --output 2>/dev/null || wl-paste 2>/dev/null",
+     `----
+  help: Consider removing this declaration.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/device/linux-system.ts:71:17]
+ 70 |     return { stdout: stdout.trim(), stderr: stderr.trim(), exitCode: 0 };
+ 71 |   } catch (err: any) {
+    :                 ^^^
+ 72 |     return {
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/device/linux-system.ts:618:33]
+ 617 |       const parsed = JSON.parse(stdout);
+ 618 |       return parsed.map((iface: any) => ({
+     :                                 ^^^
+ 619 |         name: iface.ifname,
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/device/linux-system.ts:620:41]
+ 619 |         name: iface.ifname,
+ 620 |         ipv4: iface.addr_info?.find((a: any) => a.family === "inet")?.local,
+     :                                         ^^^
+ 621 |         ipv6: iface.addr_info?.find((a: any) => a.family === "inet6")?.local,
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/device/linux-system.ts:621:41]
+ 620 |         ipv4: iface.addr_info?.find((a: any) => a.family === "inet")?.local,
+ 621 |         ipv6: iface.addr_info?.find((a: any) => a.family === "inet6")?.local,
+     :                                         ^^^
+ 622 |         mac: iface.address,
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/linux-backend.ts:47:18]
+ 46 |     execFile(cmd, args, { timeout: 10_000 }, (error) => {
+ 47 |       if (error) reject(new Error(`${cmd} failed: ${error.message}`));
+    :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 48 |       else resolve();
+    `----
+  help: Replace `reject(new Error(`${cmd} failed: ${error.message}`));` with `{reject(new Error(`${cmd} failed: ${error.message}`));}`.
+
+  x eslint(curly): Expected { after 'else'.
+    ,-[src/autonomous/device/linux-backend.ts:48:12]
+ 47 |       if (error) reject(new Error(`${cmd} failed: ${error.message}`));
+ 48 |       else resolve();
+    :            ^^^^^^^^^^
+ 49 |     });
+    `----
+  help: Replace `resolve();` with `{resolve();}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/ios-backend.ts:42:18]
+ 41 |     execFile(cmd, args, { timeout: timeoutMs }, (error, stdout, stderr) => {
+ 42 |       if (error) reject(new Error(`${cmd} ${args.join(" ")} failed: ${stderr || error.message}`));
+    :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 43 |       else resolve(stdout.trim());
+    `----
+  help: Replace `reject(new Error(`${cmd} ${args.join(" ")} failed: ${stderr || error.message}`));` with `{reject(new Error(`${cmd} ${args.join(" ")} failed: ${stderr || error.message}`));}`.
+
+  x eslint(curly): Expected { after 'else'.
+    ,-[src/autonomous/device/ios-backend.ts:43:12]
+ 42 |       if (error) reject(new Error(`${cmd} ${args.join(" ")} failed: ${stderr || error.message}`));
+ 43 |       else resolve(stdout.trim());
+    :            ^^^^^^^^^^^^^^^^^^^^^^^
+ 44 |     });
+    `----
+  help: Replace `resolve(stdout.trim());` with `{resolve(stdout.trim());}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/ios-backend.ts:83:18]
+ 82 |     req.on("error", reject);
+ 83 |     if (reqBody) req.write(reqBody);
+    :                  ^^^^^^^^^^^^^^^^^^^
+ 84 |     req.end();
+    `----
+  help: Replace `req.write(reqBody);` with `{req.write(reqBody);}`.
+
+  x eslint(no-unused-vars): Parameter 'remotePath' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/device/ios-backend.ts:477:19]
+ 476 |   /** List files in a directory on the device. */
+ 477 |   async listFiles(remotePath: string = "/"): Promise<string[]> {
+     :                   ^^^^^|^^^^
+     :                        `-- 'remotePath' is declared here
+ 478 |     const output = await run("ifuse", ["--list-apps"]).catch(() => "");
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(no-unused-vars): Variable 'output' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/device/ios-backend.ts:478:11]
+ 477 |   async listFiles(remotePath: string = "/"): Promise<string[]> {
+ 478 |     const output = await run("ifuse", ["--list-apps"]).catch(() => "");
+     :           ^^^|^^
+     :              `-- 'output' is declared here
+ 479 |     // AFC access is limited ΓÇö only Documents folders of apps and media
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Parameter 'lines' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/device/ios-backend.ts:512:19]
+ 511 |   /** Read the device syslog. */
+ 512 |   async getSyslog(lines: number = 50): Promise<string> {
+     :                   ^^|^^
+     :                     `-- 'lines' is declared here
+ 513 |     try {
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/device/android-backend.ts:90:7]
+ 89 |     if (devices.length === 0)
+ 90 |       throw new Error("No Android devices found. Connect via USB or run: adb connect <ip>:5555");
+    :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 91 |     if (!this.serial) {
+    `----
+  help: Replace `throw new Error("No Android devices found. Connect via USB or run: adb connect <ip>:5555");` with `{throw new Error("No Android devices found. Connect via USB or run: adb connect <ip>:5555");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/device/android-backend.ts:260:17]
+ 259 |       .filter(Boolean);
+ 260 |     if (filter) return packages.filter((p) => p.includes(filter));
+     :                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 261 |     return packages;
+     `----
+  help: Replace `return packages.filter((p) => p.includes(filter));` with `{return packages.filter((p) => p.includes(filter));}`.
+
+  x eslint(no-unused-vars): Function 'adbShell' is declared but never used.
+    ,-[src/autonomous/device/android-backend.ts:42:10]
+ 41 | 
+ 42 | function adbShell(command: string, timeoutMs?: number): Promise<string> {
+    :          ^^^^|^^^
+    :              `-- 'adbShell' is declared here
+ 43 |   return adb(["shell", command], timeoutMs);
+    `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Variable 'action' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/device/android-backend.ts:366:11]
+ 365 |   async toggleBluetooth(enabled: boolean): Promise<void> {
+ 366 |     const action = enabled ? "enable" : "disable";
+     :           ^^^|^^
+     :              `-- 'action' is declared here
+ 367 |     await this.shell(`settings put global bluetooth_on ${enabled ? 1 : 0}`);
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/daemon.ts:311:56]
+ 310 |   async runImprovementCycle(): Promise<void> {
+ 311 |     if (!this.memory || !this.improver || !this.agent) return;
+     :                                                        ^^^^^^^
+ 312 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(no-unused-vars): Identifier 'GoalQueue' is imported but never used.
+    ,-[src/autonomous/daemon.ts:19:10]
+ 18 | import { decideStrategy } from "./decision.js";
+ 19 | import { GoalQueue } from "./goal-queue.js";
+    :          ^^^^|^^^^
+    :              `-- 'GoalQueue' is imported here
+ 20 | import { addGridOverlay } from "./grid-overlay.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/server.ts:284:50]
+ 283 |     this.screenshotTimer = setInterval(async () => {
+ 284 |       if (this.clients.size === 0 || !this.page) return;
+     :                                                  ^^^^^^^
+ 285 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(no-unused-vars): Type 'AgentEvent' is imported but never used.
+    ,-[src/autonomous/browser-ui/server.ts:22:15]
+ 21 | import type { SelfImprover } from "../self-improve.js";
+ 22 | import type { AgentEvent, AgentState, Goal } from "../types.js";
+    :               ^^^^^|^^^^
+    :                    `-- 'AgentEvent' is imported here
+ 23 | import type { AgentUI } from "../user-interface.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Type 'AgentState' is imported but never used.
+    ,-[src/autonomous/browser-ui/server.ts:22:27]
+ 21 | import type { SelfImprover } from "../self-improve.js";
+ 22 | import type { AgentEvent, AgentState, Goal } from "../types.js";
+    :                           ^^^^^|^^^^
+    :                                `-- 'AgentState' is imported here
+ 23 | import type { AgentUI } from "../user-interface.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/browser-ui/client.js:79:41]
+ 78 |     case "screenshot":
+ 79 |       if (state.viewMode === "browser") updateBrowserView(msg.data, msg.url);
+    :                                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 80 |       break;
+    `----
+  help: Replace `updateBrowserView(msg.data, msg.url);` with `{updateBrowserView(msg.data, msg.url);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/browser-ui/client.js:83:41]
+ 82 |     case "desktop_frame":
+ 83 |       if (state.viewMode === "desktop") updateDesktopView(msg.data);
+    :                                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 84 |       break;
+    `----
+  help: Replace `updateDesktopView(msg.data);` with `{updateDesktopView(msg.data);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:133:12]
+ 132 |   const dot = document.getElementById("connectionDot");
+ 133 |   if (dot) dot.className = `connection-dot ${connected ? "connected" : ""}`;
+     :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 134 | }
+     `----
+  help: Replace `dot.className = `connection-dot ${connected ? "connected" : ""}`;` with `{dot.className = `connection-dot ${connected ? "connected" : ""}`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:141:14]
+ 140 |   const text = document.getElementById("statusText");
+ 141 |   if (badge) badge.className = `status-badge ${agentState}`;
+     :              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 142 |   if (dot) dot.className = `status-dot ${agentState}`;
+     `----
+  help: Replace `badge.className = `status-badge ${agentState}`;` with `{badge.className = `status-badge ${agentState}`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:142:12]
+ 141 |   if (badge) badge.className = `status-badge ${agentState}`;
+ 142 |   if (dot) dot.className = `status-dot ${agentState}`;
+     :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 143 |   if (text) text.textContent = agentState.toUpperCase();
+     `----
+  help: Replace `dot.className = `status-dot ${agentState}`;` with `{dot.className = `status-dot ${agentState}`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:143:13]
+ 142 |   if (dot) dot.className = `status-dot ${agentState}`;
+ 143 |   if (text) text.textContent = agentState.toUpperCase();
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 144 | }
+     `----
+  help: Replace `text.textContent = agentState.toUpperCase();` with `{text.textContent = agentState.toUpperCase();}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:153:20]
+ 152 |   }
+ 153 |   if (placeholder) placeholder.style.display = "none";
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 154 |   if (url) {
+     `----
+  help: Replace `placeholder.style.display = "none";` with `{placeholder.style.display = "none";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:156:19]
+ 155 |     const urlInput = document.getElementById("urlInput");
+ 156 |     if (urlInput) urlInput.value = url;
+     :                   ^^^^^^^^^^^^^^^^^^^^^
+ 157 |   }
+     `----
+  help: Replace `urlInput.value = url;` with `{urlInput.value = url;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:167:20]
+ 166 |   }
+ 167 |   if (placeholder) placeholder.style.display = "none";
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 168 | }
+     `----
+  help: Replace `placeholder.style.display = "none";` with `{placeholder.style.display = "none";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:174:17]
+ 173 |   const reasoning = document.getElementById("actionReasoning");
+ 174 |   if (!overlay) return;
+     :                 ^^^^^^^
+ 175 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:177:13]
+ 176 |   overlay.classList.add("visible");
+ 177 |   if (text) text.textContent = `${msg.action || ""}`;
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 178 |   if (reasoning) reasoning.textContent = msg.thinking || msg.reasoning || "";
+     `----
+  help: Replace `text.textContent = `${msg.action || ""}`;` with `{text.textContent = `${msg.action || ""}`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:178:18]
+ 177 |   if (text) text.textContent = `${msg.action || ""}`;
+ 178 |   if (reasoning) reasoning.textContent = msg.thinking || msg.reasoning || "";
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 179 | 
+     `----
+  help: Replace `reasoning.textContent = msg.thinking || msg.reasoning || "";` with `{reasoning.textContent = msg.thinking || msg.reasoning || "";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:187:18]
+ 186 | function updateFullState(msg) {
+ 187 |   if (msg.state) updateAgentState(msg.state);
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 188 |   if (msg.goalCount !== undefined) {
+     `----
+  help: Replace `updateAgentState(msg.state);` with `{updateAgentState(msg.state);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:190:13]
+ 189 |     const el = document.getElementById("goalCount");
+ 190 |     if (el) el.textContent = msg.goalCount;
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 191 |   }
+     `----
+  help: Replace `el.textContent = msg.goalCount;` with `{el.textContent = msg.goalCount;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:194:13]
+ 193 |     const el = document.getElementById("memoryCount");
+ 194 |     if (el) el.textContent = msg.memoryCount;
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 195 |   }
+     `----
+  help: Replace `el.textContent = msg.memoryCount;` with `{el.textContent = msg.memoryCount;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:198:13]
+ 197 |     const el = document.getElementById("rulesCount");
+ 198 |     if (el) el.textContent = msg.rulesCount;
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 199 |   }
+     `----
+  help: Replace `el.textContent = msg.rulesCount;` with `{el.textContent = msg.rulesCount;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:212:14]
+ 211 |   const list = document.getElementById("goalList");
+ 212 |   if (!list) return;
+     :              ^^^^^^^
+ 213 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:243:29]
+ 242 |   const priority = document.getElementById("goalPriority");
+ 243 |   if (!input?.value.trim()) return;
+     :                             ^^^^^^^
+ 244 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:259:29]
+ 258 |   const input = document.getElementById("commandInput");
+ 259 |   if (!input?.value.trim()) return;
+     :                             ^^^^^^^
+ 260 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:292:16]
+ 291 |     const modal = document.getElementById("approvalModal");
+ 292 |     if (modal) modal.style.display = "none";
+     :                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 293 |     return;
+     `----
+  help: Replace `modal.style.display = "none";` with `{modal.style.display = "none";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:301:14]
+ 300 | 
+ 301 |   if (modal) modal.style.display = "flex";
+     :              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 302 |   if (desc) desc.textContent = req.description;
+     `----
+  help: Replace `modal.style.display = "flex";` with `{modal.style.display = "flex";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:302:13]
+ 301 |   if (modal) modal.style.display = "flex";
+ 302 |   if (desc) desc.textContent = req.description;
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 303 |   if (risk) {
+     `----
+  help: Replace `desc.textContent = req.description;` with `{desc.textContent = req.description;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:310:41]
+ 309 | function resolveApproval(approved, alwaysAllow = false) {
+ 310 |   if (state.approvalQueue.length === 0) return;
+     :                                         ^^^^^^^
+ 311 |   const req = state.approvalQueue.shift();
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:366:18]
+ 365 |       const input = document.getElementById("commandInput");
+ 366 |       if (input) input.value = finalTranscript;
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 367 |     }
+     `----
+  help: Replace `input.value = finalTranscript;` with `{input.value = finalTranscript;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:408:32]
+ 407 | function speak(text, rate = 1.0) {
+ 408 |   if (!window.speechSynthesis) return;
+     :                                ^^^^^^^
+ 409 |   const utterance = new SpeechSynthesisUtterance(text);
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:423:19]
+ 422 |   const browserBtn = document.getElementById("viewBrowser");
+ 423 |   if (desktopBtn) desktopBtn.classList.toggle("active", mode === "desktop");
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 424 |   if (browserBtn) browserBtn.classList.toggle("active", mode === "browser");
+     `----
+  help: Replace `desktopBtn.classList.toggle("active", mode === "desktop");` with `{desktopBtn.classList.toggle("active", mode === "desktop");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:424:19]
+ 423 |   if (desktopBtn) desktopBtn.classList.toggle("active", mode === "desktop");
+ 424 |   if (browserBtn) browserBtn.classList.toggle("active", mode === "browser");
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 425 | 
+     `----
+  help: Replace `browserBtn.classList.toggle("active", mode === "browser");` with `{browserBtn.classList.toggle("active", mode === "browser");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:438:16]
+ 437 |     const name = prompt("Save workflow as:", "My Workflow");
+ 438 |     if (!name) return;
+     :                ^^^^^^^
+ 439 |     send({ type: "workflow_stop", name });
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:467:15]
+ 466 |   const panel = document.getElementById(`tab-${tab}`);
+ 467 |   if (tabBtn) tabBtn.classList.add("active");
+     :               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 468 |   if (panel) panel.classList.add("active");
+     `----
+  help: Replace `tabBtn.classList.add("active");` with `{tabBtn.classList.add("active");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:468:14]
+ 467 |   if (tabBtn) tabBtn.classList.add("active");
+ 468 |   if (panel) panel.classList.add("active");
+     :              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 469 | }
+     `----
+  help: Replace `panel.classList.add("active");` with `{panel.classList.add("active");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:477:14]
+ 476 |   const list = document.getElementById("logList");
+ 477 |   if (!list) return;
+     :              ^^^^^^^
+ 478 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:506:29]
+ 505 |   const input = document.getElementById("messageInput");
+ 506 |   if (!input?.value.trim()) return;
+     :                             ^^^^^^^
+ 507 |   send({ type: "user_message", text: input.value });
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/browser-ui/client.js:513:29]
+ 512 |   const input = document.getElementById("urlInput");
+ 513 |   if (!input?.value.trim()) return;
+     :                             ^^^^^^^
+ 514 |   send({ type: "navigate", url: input.value });
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(no-unused-vars): Variable 'reconnectTimer' is assigned a value but never used. Unused variables should start with a '_'.
+    ,-[src/autonomous/browser-ui/client.js:20:5]
+ 19 | let ws = null;
+ 20 | let reconnectTimer = null;
+    :     ^^^^^^^|^^^^^^
+    :            `-- 'reconnectTimer' is declared here
+ 21 | const state = {
+    `----
+    ,-[src/autonomous/browser-ui/client.js:49:5]
+ 48 |     updateConnectionDot(false);
+ 49 |     reconnectTimer = setTimeout(connect, 3000);
+    :     ^^^^^^^|^^^^^^
+    :            `-- it was last assigned here
+ 50 |   };
+    `----
+  help: Did you mean to use this variable?
+
+  x eslint(no-unused-vars): Function 'addGoal' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:240:10]
+ 239 | 
+ 240 | function addGoal() {
+     :          ^^^|^^^
+     :             `-- 'addGoal' is declared here
+ 241 |   const input = document.getElementById("goalInput");
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'cancelGoal' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:249:10]
+ 248 | 
+ 249 | function cancelGoal(id) {
+     :          ^^^^^|^^^^
+     :               `-- 'cancelGoal' is declared here
+ 250 |   send({ type: "cancel_goal", goalId: id });
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'sendCommand' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:257:10]
+ 256 | 
+ 257 | function sendCommand() {
+     :          ^^^^^|^^^^^
+     :               `-- 'sendCommand' is declared here
+ 258 |   const input = document.getElementById("commandInput");
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'resolveApproval' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:309:10]
+ 308 | 
+ 309 | function resolveApproval(approved, alwaysAllow = false) {
+     :          ^^^^^^^|^^^^^^^
+     :                 `-- 'resolveApproval' is declared here
+ 310 |   if (state.approvalQueue.length === 0) return;
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'toggleVoice' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:327:10]
+ 326 | 
+ 327 | function toggleVoice() {
+     :          ^^^^^|^^^^^
+     :               `-- 'toggleVoice' is declared here
+ 328 |   if (!("webkitSpeechRecognition" in window) && !("SpeechRecognition" in window)) {
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'setViewMode' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:419:10]
+ 418 | 
+ 419 | function setViewMode(mode) {
+     :          ^^^^^|^^^^^
+     :               `-- 'setViewMode' is declared here
+ 420 |   state.viewMode = mode;
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'toggleRecording' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:434:10]
+ 433 | 
+ 434 | function toggleRecording() {
+     :          ^^^^^^^|^^^^^^^
+     :                 `-- 'toggleRecording' is declared here
+ 435 |   if (state.recording) {
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'switchTab' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:461:10]
+ 460 | 
+ 461 | function switchTab(tab) {
+     :          ^^^^|^^^^
+     :              `-- 'switchTab' is declared here
+ 462 |   document.querySelectorAll(".sidebar-tab").forEach((t) => t.classList.remove("active"));
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'sendMessage' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:504:10]
+ 503 | 
+ 504 | function sendMessage() {
+     :          ^^^^^|^^^^^
+     :               `-- 'sendMessage' is declared here
+ 505 |   const input = document.getElementById("messageInput");
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'navigateToUrl' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:511:10]
+ 510 | 
+ 511 | function navigateToUrl() {
+     :          ^^^^^^|^^^^^^
+     :                `-- 'navigateToUrl' is declared here
+ 512 |   const input = document.getElementById("urlInput");
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'navigateBack' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:517:10]
+ 516 | 
+ 517 | function navigateBack() {
+     :          ^^^^^^|^^^^^
+     :                `-- 'navigateBack' is declared here
+ 518 |   send({ type: "navigate_back" });
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'navigateForward' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:520:10]
+ 519 | }
+ 520 | function navigateForward() {
+     :          ^^^^^^^|^^^^^^^
+     :                 `-- 'navigateForward' is declared here
+ 521 |   send({ type: "navigate_forward" });
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'reloadPage' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:523:10]
+ 522 | }
+ 523 | function reloadPage() {
+     :          ^^^^^|^^^^
+     :               `-- 'reloadPage' is declared here
+ 524 |   send({ type: "reload" });
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Function 'runImprovement' is declared but never used.
+     ,-[src/autonomous/browser-ui/client.js:526:10]
+ 525 | }
+ 526 | function runImprovement() {
+     :          ^^^^^^^|^^^^^^
+     :                 `-- 'runImprovement' is declared here
+ 527 |   send({ type: "run_improvement" });
+     `----
+  help: Consider removing this declaration.
+
+  x eslint-plugin-unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
+    ,-[src/autonomous/browser-ui/client.js:40:6]
+ 39 | 
+ 40 |   ws.onopen = () => {
+    :      ^^^^^^
+ 41 |     state.connected = true;
+    `----
+
+  x eslint-plugin-unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
+    ,-[src/autonomous/browser-ui/client.js:46:6]
+ 45 | 
+ 46 |   ws.onclose = () => {
+    :      ^^^^^^^
+ 47 |     state.connected = false;
+    `----
+
+  x eslint-plugin-unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
+    ,-[src/autonomous/browser-ui/client.js:52:6]
+ 51 | 
+ 52 |   ws.onmessage = (event) => {
+    :      ^^^^^^^^^
+ 53 |     try {
+    `----
+
+  x eslint-plugin-unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
+     ,-[src/autonomous/browser-ui/client.js:370:15]
+ 369 | 
+ 370 |   recognition.onerror = (event) => {
+     :               ^^^^^^^
+ 371 |     console.error("Voice error:", event.error);
+     `----
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:42:28]
+ 41 |   // CLI flag overrides
+ 42 |   if (opts?.forceDisabled) return;
+    :                            ^^^^^^^
+ 43 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:45:17]
+ 44 |   const enabled = opts?.forceEnabled ?? autonomousConfig?.enabled;
+ 45 |   if (!enabled) return;
+    :                 ^^^^^^^
+ 46 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:57:36]
+ 56 | 
+ 57 |     if (autonomousConfig?.dataDir) config.dataDir = autonomousConfig.dataDir;
+    :                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 58 | 
+    `----
+  help: Replace `config.dataDir = autonomousConfig.dataDir;` with `{config.dataDir = autonomousConfig.dataDir;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:78:26]
+ 77 | export async function stopAutonomous(): Promise<void> {
+ 78 |   if (!autonomousDaemon) return;
+    :                          ^^^^^^^
+ 79 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:13:23]
+ 12 | 
+ 13 | let autonomousDaemon: any = null;
+    :                       ^^^
+ 14 | 
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/autonomous-gateway-hook.ts:60:52]
+ 59 |     log.info("starting autonomous agent...");
+ 60 |     autonomousDaemon = await startDaemon(config as any);
+    :                                                    ^^^
+ 61 | 
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/auth-manager.ts:71:22]
+ 70 |             const key = localStorage.key(i);
+ 71 |             if (key) data[key] = localStorage.getItem(key) ?? "";
+    :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 72 |           }
+    `----
+  help: Replace `data[key] = localStorage.getItem(key) ?? "";` with `{data[key] = localStorage.getItem(key) ?? "";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/auth-manager.ts:101:27]
+ 100 |       this.snapshot = JSON.parse(raw);
+ 101 |       if (!this.snapshot) return false;
+     :                           ^^^^^^^^^^^^^
+ 102 | 
+     `----
+  help: Replace `return false;` with `{return false;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/auth-manager.ts:147:25]
+ 146 |   getInfo(): { savedAt: string; cookieCount: number; originCount: number } | null {
+ 147 |     if (!this.snapshot) return null;
+     :                         ^^^^^^^^^^^^
+ 148 |     return {
+     `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/approval.ts:184:19]
+ 183 |     const request = this.pending.get(requestId);
+ 184 |     if (!request) return;
+     :                   ^^^^^^^
+ 185 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/approval.ts:256:28]
+ 255 |     const key = this.getActionKey(action);
+ 256 |     if (this.riskMap[key]) return this.riskMap[key];
+     :                            ^^^^^^^^^^^^^^^^^^^^^^^^^
+ 257 | 
+     `----
+  help: Replace `return this.riskMap[key];` with `{return this.riskMap[key];}`.
+
+  x eslint(no-unused-vars): Type 'DeviceController' is imported but never used.
+    ,-[src/autonomous/ai-delegation.ts:14:15]
+ 13 | 
+ 14 | import type { DeviceController } from "./device-controller.js";
+    :               ^^^^^^^^|^^^^^^^
+    :                       `-- 'DeviceController' is imported here
+ 15 | import type { NLCommander, CommandResult } from "./nl-commander.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/workflow-replay.ts:96:25]
+ 95 |   startRecording(): void {
+ 96 |     if (this.recording) throw new Error("Already recording");
+    :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 97 |     this.recording = true;
+    `----
+  help: Replace `throw new Error("Already recording");` with `{throw new Error("Already recording");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/workflow-replay.ts:105:26]
+ 104 |   recordAction(action: DeviceAction, screenshot?: Buffer): void {
+ 105 |     if (!this.recording) return;
+     :                          ^^^^^^^
+ 106 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/workflow-replay.ts:125:26]
+ 124 |   stopRecording(name: string, description: string = "", tags: string[] = []): SavedWorkflow {
+ 125 |     if (!this.recording) throw new Error("Not recording");
+     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 126 | 
+     `----
+  help: Replace `throw new Error("Not recording");` with `{throw new Error("Not recording");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/workflow-replay.ts:176:20]
+ 175 |     const workflow = this.workflows.get(workflowId);
+ 176 |     if (!workflow) throw new Error(`Workflow not found: ${workflowId}`);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 177 |     if (!this.controller) throw new Error("Controller not initialized");
+     `----
+  help: Replace `throw new Error(`Workflow not found: ${workflowId}`);` with `{throw new Error(`Workflow not found: ${workflowId}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/workflow-replay.ts:177:27]
+ 176 |     if (!workflow) throw new Error(`Workflow not found: ${workflowId}`);
+ 177 |     if (!this.controller) throw new Error("Controller not initialized");
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 178 | 
+     `----
+  help: Replace `throw new Error("Controller not initialized");` with `{throw new Error("Controller not initialized");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/workflow-replay.ts:280:20]
+ 279 |     const workflow = this.workflows.get(workflowId);
+ 280 |     if (!workflow) throw new Error(`Workflow not found: ${workflowId}`);
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 281 |     return JSON.stringify(workflow, null, 2);
+     `----
+  help: Replace `throw new Error(`Workflow not found: ${workflowId}`);` with `{throw new Error(`Workflow not found: ${workflowId}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/vision.ts:235:32]
+ 234 |     }
+ 235 |     if (suggestedAction?.text) action.text = String(suggestedAction.text);
+     :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+     `----
+  help: Replace `action.text = String(suggestedAction.text);` with `{action.text = String(suggestedAction.text);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/vision.ts:236:31]
+ 235 |     if (suggestedAction?.text) action.text = String(suggestedAction.text);
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+     :                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 237 |     if (suggestedAction?.key) action.key = String(suggestedAction.key);
+     `----
+  help: Replace `action.url = String(suggestedAction.url);` with `{action.url = String(suggestedAction.url);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/vision.ts:237:31]
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+ 237 |     if (suggestedAction?.key) action.key = String(suggestedAction.key);
+     :                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 238 |     if (suggestedAction?.direction) action.direction = suggestedAction.direction as "up" | "down";
+     `----
+  help: Replace `action.key = String(suggestedAction.key);` with `{action.key = String(suggestedAction.key);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/vision.ts:238:37]
+ 237 |     if (suggestedAction?.key) action.key = String(suggestedAction.key);
+ 238 |     if (suggestedAction?.direction) action.direction = suggestedAction.direction as "up" | "down";
+     :                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 239 |     if (suggestedAction?.durationMs) action.durationMs = Number(suggestedAction.durationMs);
+     `----
+  help: Replace `action.direction = suggestedAction.direction as "up" | "down";` with `{action.direction = suggestedAction.direction as "up" | "down";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/vision.ts:239:38]
+ 238 |     if (suggestedAction?.direction) action.direction = suggestedAction.direction as "up" | "down";
+ 239 |     if (suggestedAction?.durationMs) action.durationMs = Number(suggestedAction.durationMs);
+     :                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 240 | 
+     `----
+  help: Replace `action.durationMs = Number(suggestedAction.durationMs);` with `{action.durationMs = Number(suggestedAction.durationMs);}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/user-profile.ts:154:8]
+ 153 |     return Object.values(this.data.apps)
+ 154 |       .sort((a, b) => b.launchCount - a.launchCount)
+     :        ^^^^
+ 155 |       .slice(0, limit);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/user-profile.ts:162:8]
+ 161 |       .filter((app) => app.timeSlots[hour] > 0)
+ 162 |       .sort((a, b) => b.timeSlots[hour]! - a.timeSlots[hour]!)
+     :        ^^^^
+ 163 |       .slice(0, 5);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/user-profile.ts:206:8]
+ 205 |       )
+ 206 |       .sort((a, b) => b.confidence - a.confidence)
+     :        ^^^^
+ 207 |       .slice(0, 5);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/user-profile.ts:252:8]
+ 251 |     return this.data.commandHistory
+ 252 |       .sort((a, b) => b.count - a.count)
+     :        ^^^^
+ 253 |       .slice(0, limit)
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
+     ,-[src/autonomous/user-profile.ts:141:20]
+ 140 |         lastUsed: 0,
+ 141 |         timeSlots: new Array(24).fill(0),
+     :                    ^^^^^^^^^^^^^
+ 142 |       };
+     `----
+  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/task-scheduler.ts:59:24]
+ 58 |   start(): void {
+ 59 |     if (this._running) return;
+    :                        ^^^^^^^
+ 60 |     this._running = true;
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/task-scheduler.ts:65:25]
+ 64 |     for (const task of Array.from(this.tasks.values())) {
+ 65 |       if (task.enabled) this.scheduleNext(task);
+    :                         ^^^^^^^^^^^^^^^^^^^^^^^^
+ 66 |     }
+    `----
+  help: Replace `this.scheduleNext(task);` with `{this.scheduleNext(task);}`.
+
+  x eslint(curly): Expected { after 'for-of'.
+    ,-[src/autonomous/task-scheduler.ts:72:59]
+ 71 |     this._running = false;
+ 72 |     for (const timer of Array.from(this.timers.values())) clearTimeout(timer);
+    :                                                           ^^^^^^^^^^^^^^^^^^^^
+ 73 |     this.timers.clear();
+    `----
+  help: Replace `clearTimeout(timer);` with `{clearTimeout(timer);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/task-scheduler.ts:98:24]
+ 97 |     this.tasks.set(id, task);
+ 98 |     if (this._running) this.scheduleNext(task);
+    :                        ^^^^^^^^^^^^^^^^^^^^^^^^
+ 99 |     log.info(`added task "${name}" (${id}), next run: ${new Date(task.nextRun).toLocaleString()}`);
+    `----
+  help: Replace `this.scheduleNext(task);` with `{this.scheduleNext(task);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:127:16]
+ 126 |     const timer = this.timers.get(taskId);
+ 127 |     if (timer) clearTimeout(timer);
+     :                ^^^^^^^^^^^^^^^^^^^^
+ 128 |     this.timers.delete(taskId);
+     `----
+  help: Replace `clearTimeout(timer);` with `{clearTimeout(timer);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:135:16]
+ 134 |     const task = this.tasks.get(taskId);
+ 135 |     if (!task) return;
+     :                ^^^^^^^
+ 136 |     task.enabled = enabled;
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:141:18]
+ 140 |       const timer = this.timers.get(taskId);
+ 141 |       if (timer) clearTimeout(timer);
+     :                  ^^^^^^^^^^^^^^^^^^^^
+ 142 |       this.timers.delete(taskId);
+     `----
+  help: Replace `clearTimeout(timer);` with `{clearTimeout(timer);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:170:20]
+ 169 |     let delay = task.nextRun - now;
+ 170 |     if (delay < 0) delay = 1000; // Run immediately if overdue
+     :                    ^^^^^^^^^^^^^
+ 171 | 
+     `----
+  help: Replace `delay = 1000;` with `{delay = 1000;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:180:42]
+ 179 |   private async executeTask(task: ScheduledTask): Promise<void> {
+ 180 |     if (!task.enabled || !this._running) return;
+     :                                          ^^^^^^^
+ 181 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/task-scheduler.ts:218:43]
+ 217 |         next.setHours(schedule.hour, schedule.minute, 0, 0);
+ 218 |         if (next.getTime() <= Date.now()) next.setDate(next.getDate() + 1);
+     :                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 219 |         return next.getTime();
+     `----
+  help: Replace `next.setDate(next.getDate() + 1);` with `{next.setDate(next.getDate() + 1);}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/task-scheduler.ts:155:8]
+ 154 |       .filter((t) => t.enabled)
+ 155 |       .sort((a, b) => a.nextRun - b.nextRun)
+     :        ^^^^
+ 156 |       .slice(0, limit);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/tab-manager.ts:55:24]
+ 54 |   async openTab(url?: string): Promise<Tab> {
+ 55 |     if (!this.context) throw new Error("TabManager not initialized");
+    :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 56 | 
+    `----
+  help: Replace `throw new Error("TabManager not initialized");` with `{throw new Error("TabManager not initialized");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/tab-manager.ts:81:15]
+ 80 |     const tab = this.tabs.get(tabId);
+ 81 |     if (!tab) return null;
+    :               ^^^^^^^^^^^^
+ 82 | 
+    `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/tab-manager.ts:86:20]
+ 85 |       const current = this.tabs.get(this.activeTabId);
+ 86 |       if (current) current.active = false;
+    :                    ^^^^^^^^^^^^^^^^^^^^^^^
+ 87 |     }
+    `----
+  help: Replace `current.active = false;` with `{current.active = false;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/tab-manager.ts:97:15]
+ 96 |     const tab = this.tabs.get(tabId);
+ 97 |     if (!tab) return null;
+    :               ^^^^^^^^^^^^
+ 98 | 
+    `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/tab-manager.ts:116:28]
+ 115 |   getActivePage(): Page | null {
+ 116 |     if (!this.activeTabId) return null;
+     :                            ^^^^^^^^^^^^
+ 117 |     return this.tabs.get(this.activeTabId)?.page ?? null;
+     `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/smart-clipboard.ts:47:25]
+ 46 |   async startMonitoring(intervalMs: number = 1000): Promise<void> {
+ 47 |     if (this.pollTimer) return;
+    :                         ^^^^^^^
+ 48 |     if (!this.controller?.system) {
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:117:16]
+ 116 |     const entry = this.history.find((e) => e.id === entryId);
+ 117 |     if (entry) entry.pinned = true;
+     :                ^^^^^^^^^^^^^^^^^^^^
+ 118 |   }
+     `----
+  help: Replace `entry.pinned = true;` with `{entry.pinned = true;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:123:17]
+ 122 |     const entry = this.history.find((e) => e.id === entryId);
+ 123 |     if (!entry) throw new Error("Entry not found");
+     :                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 124 |     if (!this.controller?.system) throw new Error("System controller required");
+     `----
+  help: Replace `throw new Error("Entry not found");` with `{throw new Error("Entry not found");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:124:35]
+ 123 |     if (!entry) throw new Error("Entry not found");
+ 124 |     if (!this.controller?.system) throw new Error("System controller required");
+     :                                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 125 |     await this.controller.system.clipboardCopy(entry.text);
+     `----
+  help: Replace `throw new Error("System controller required");` with `{throw new Error("System controller required");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:132:27]
+ 131 |   async paste(): Promise<void> {
+ 132 |     if (!this.controller) throw new Error("Controller required");
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 133 |     await this.controller.hotkey(["ctrl"], "v");
+     `----
+  help: Replace `throw new Error("Controller required");` with `{throw new Error("Controller required");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:157:38]
+ 156 |     const tags: string[] = [];
+ 157 |     if (/https?:\/\/\S+/.test(text)) tags.push("url");
+     :                                      ^^^^^^^^^^^^^^^^^
+ 158 |     if (/[\w.+-]+@[\w-]+\.[\w.]+/.test(text)) tags.push("email");
+     `----
+  help: Replace `tags.push("url");` with `{tags.push("url");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:158:47]
+ 157 |     if (/https?:\/\/\S+/.test(text)) tags.push("url");
+ 158 |     if (/[\w.+-]+@[\w-]+\.[\w.]+/.test(text)) tags.push("email");
+     :                                               ^^^^^^^^^^^^^^^^^^^
+ 159 |     if (/^\d+$/.test(text.trim())) tags.push("number");
+     `----
+  help: Replace `tags.push("email");` with `{tags.push("email");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:159:36]
+ 158 |     if (/[\w.+-]+@[\w-]+\.[\w.]+/.test(text)) tags.push("email");
+ 159 |     if (/^\d+$/.test(text.trim())) tags.push("number");
+     :                                    ^^^^^^^^^^^^^^^^^^^^
+ 160 |     if (/\{[\s\S]*\}/.test(text) || /\[[\s\S]*\]/.test(text)) tags.push("json");
+     `----
+  help: Replace `tags.push("number");` with `{tags.push("number");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:160:63]
+ 159 |     if (/^\d+$/.test(text.trim())) tags.push("number");
+ 160 |     if (/\{[\s\S]*\}/.test(text) || /\[[\s\S]*\]/.test(text)) tags.push("json");
+     :                                                               ^^^^^^^^^^^^^^^^^^
+ 161 |     if (text.includes("function ") || text.includes("const ") || text.includes("import "))
+     `----
+  help: Replace `tags.push("json");` with `{tags.push("json");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:162:7]
+ 161 |     if (text.includes("function ") || text.includes("const ") || text.includes("import "))
+ 162 |       tags.push("code");
+     :       ^^^^^^^^^^^^^^^^^^
+ 163 |     if (/^\/[\w/.-]+$/.test(text.trim())) tags.push("path");
+     `----
+  help: Replace `tags.push("code");` with `{tags.push("code");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:163:43]
+ 162 |       tags.push("code");
+ 163 |     if (/^\/[\w/.-]+$/.test(text.trim())) tags.push("path");
+     :                                           ^^^^^^^^^^^^^^^^^^
+ 164 |     if (text.length > 500) tags.push("long");
+     `----
+  help: Replace `tags.push("path");` with `{tags.push("path");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/smart-clipboard.ts:164:28]
+ 163 |     if (/^\/[\w/.-]+$/.test(text.trim())) tags.push("path");
+ 164 |     if (text.length > 500) tags.push("long");
+     :                            ^^^^^^^^^^^^^^^^^^
+ 165 |     return tags;
+     `----
+  help: Replace `tags.push("long");` with `{tags.push("long");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-improve.ts:170:33]
+ 169 |       .filter((r) => {
+ 170 |         if (r.confidence < 0.3) return false;
+     :                                 ^^^^^^^^^^^^^
+ 171 |         if (r.appliesTo === "*") return true;
+     `----
+  help: Replace `return false;` with `{return false;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-improve.ts:171:34]
+ 170 |         if (r.confidence < 0.3) return false;
+ 171 |         if (r.appliesTo === "*") return true;
+     :                                  ^^^^^^^^^^^^
+ 172 |         if (!goal) return r.appliesTo === "*";
+     `----
+  help: Replace `return true;` with `{return true;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-improve.ts:172:20]
+ 171 |         if (r.appliesTo === "*") return true;
+ 172 |         if (!goal) return r.appliesTo === "*";
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 173 |         if (r.appliesTo && goal.description.toLowerCase().includes(r.appliesTo.toLowerCase()))
+     `----
+  help: Replace `return r.appliesTo === "*";` with `{return r.appliesTo === "*";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-improve.ts:174:11]
+ 173 |         if (r.appliesTo && goal.description.toLowerCase().includes(r.appliesTo.toLowerCase()))
+ 174 |           return true;
+     :           ^^^^^^^^^^^^
+ 175 |         return false;
+     `----
+  help: Replace `return true;` with `{return true;}`.
+
+  x eslint(no-unused-vars): Variable 'stats' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/self-improve.ts:389:11]
+ 388 |     // A goal with high retry count but eventual success = candidate for decomposition
+ 389 |     const stats = memory.getStats();
+     :           ^^|^^
+     :             `-- 'stats' is declared here
+ 390 | 
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Variable 'before' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/self-improve.ts:480:11]
+ 479 |     const changes: string[] = [];
+ 480 |     const before = this.state.learnedRules.length;
+     :           ^^^|^^
+     :              `-- 'before' is declared here
+ 481 | 
+     `----
+  help: Consider removing this declaration.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/self-improve.ts:177:8]
+ 176 |       })
+ 177 |       .sort((a, b) => b.effectivenessScore - a.effectivenessScore)
+     :        ^^^^
+ 178 |       .slice(0, 10);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:272:25]
+ 271 |   for (const [goalId, eps] of goalEpisodes) {
+ 272 |     if (eps.length < 2) continue;
+     :                         ^^^^^^^^^
+ 273 |     const sorted = eps.sort(
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:294:34]
+ 293 |     d.total += 1;
+ 294 |     if (ep.result === "success") d.successes += 1;
+     :                                  ^^^^^^^^^^^^^^^^^
+ 295 |     d.goalIds.add(ep.goalId);
+     `----
+  help: Replace `d.successes += 1;` with `{d.successes += 1;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:337:42]
+ 336 |         s.totalRetries += goal.retries;
+ 337 |         if (goal.status === "completed") s.successes += 1;
+     :                                          ^^^^^^^^^^^^^^^^^
+ 338 |       }
+     `----
+  help: Replace `s.successes += 1;` with `{s.successes += 1;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:347:29]
+ 346 |     let recommendation: "keep" | "adjust" | "deprecate";
+ 347 |     if (successRate >= 0.7) recommendation = "keep";
+     :                             ^^^^^^^^^^^^^^^^^^^^^^^^
+ 348 |     else if (successRate >= 0.3) recommendation = "adjust";
+     `----
+  help: Replace `recommendation = "keep";` with `{recommendation = "keep";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:348:34]
+ 347 |     if (successRate >= 0.7) recommendation = "keep";
+ 348 |     else if (successRate >= 0.3) recommendation = "adjust";
+     :                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 349 |     else recommendation = "deprecate";
+     `----
+  help: Replace `recommendation = "adjust";` with `{recommendation = "adjust";}`.
+
+  x eslint(curly): Expected { after 'else'.
+     ,-[src/autonomous/self-analyze.ts:349:10]
+ 348 |     else if (successRate >= 0.3) recommendation = "adjust";
+ 349 |     else recommendation = "deprecate";
+     :          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 350 | 
+     `----
+  help: Replace `recommendation = "deprecate";` with `{recommendation = "deprecate";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/self-analyze.ts:520:28]
+ 519 | function mean(values: number[]): number {
+ 520 |   if (values.length === 0) return 0;
+     :                            ^^^^^^^^^
+ 521 |   return values.reduce((sum, v) => sum + v, 0) / values.length;
+     `----
+  help: Replace `return 0;` with `{return 0;}`.
+
+  x eslint(no-unused-vars): Type 'LearnedProcedure' is imported but never used.
+    ,-[src/autonomous/self-analyze.ts:15:30]
+ 14 | 
+ 15 | import type { EpisodicEntry, LearnedProcedure, FailurePattern, AgentMemory } from "./memory.js";
+    :                              ^^^^^^^^|^^^^^^^
+    :                                      `-- 'LearnedProcedure' is imported here
+ 16 | import type { ErrorType, Goal, GoalPriority } from "./types.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Type 'FailurePattern' is imported but never used.
+    ,-[src/autonomous/self-analyze.ts:15:48]
+ 14 | 
+ 15 | import type { EpisodicEntry, LearnedProcedure, FailurePattern, AgentMemory } from "./memory.js";
+    :                                                ^^^^^^^|^^^^^^
+    :                                                       `-- 'FailurePattern' is imported here
+ 16 | import type { ErrorType, Goal, GoalPriority } from "./types.js";
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Type 'GoalPriority' is imported but never used.
+    ,-[src/autonomous/self-analyze.ts:16:32]
+ 15 | import type { EpisodicEntry, LearnedProcedure, FailurePattern, AgentMemory } from "./memory.js";
+ 16 | import type { ErrorType, Goal, GoalPriority } from "./types.js";
+    :                                ^^^^^^|^^^^^
+    :                                      `-- 'GoalPriority' is imported here
+ 17 | 
+    `----
+  help: Consider removing this import.
+
+  x eslint(no-unused-vars): Variable 'stats' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/self-analyze.ts:101:9]
+ 100 | 
+ 101 |   const stats = memory.getStats();
+     :         ^^|^^
+     :           `-- 'stats' is declared here
+ 102 | 
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Variable 'stats' is declared but never used. Unused variables should start with a '_'.
+     ,-[src/autonomous/self-analyze.ts:360:9]
+ 359 |   const failedEpisodes = episodes.filter((e) => e.result === "failure");
+ 360 |   const stats = memory.getStats();
+     :         ^^|^^
+     :           `-- 'stats' is declared here
+ 361 | 
+     `----
+  help: Consider removing this declaration.
+
+  x eslint(no-unused-vars): Parameter 'errorAnalysis' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/self-analyze.ts:412:3]
+ 411 |   domainProfiles: DomainProfile[],
+ 412 |   errorAnalysis: PerformanceReport["errorAnalysis"],
+     :   ^^^^^^|^^^^^^
+     :         `-- 'errorAnalysis' is declared here
+ 413 | ): PerformanceReport["bottlenecks"] {
+     `----
+  help: Consider removing this parameter.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/self-analyze.ts:273:24]
+ 272 |     if (eps.length < 2) continue;
+ 273 |     const sorted = eps.sort(
+     :                        ^^^^
+ 274 |       (a, b) => new Date(a.timestamp).getTime() - new Date(b.timestamp).getTime(),
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/self-analyze.ts:302:8]
+ 301 |     const commonErrors = failures
+ 302 |       .sort((a, b) => b.count - a.count)
+     :        ^^^^
+ 303 |       .slice(0, 3)
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/self-analyze.ts:316:19]
+ 315 | 
+ 316 |   return profiles.sort((a, b) => a.successRate - b.successRate);
+     :                   ^^^^
+ 317 | }
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/self-analyze.ts:395:6]
+ 394 |   const topUrls = [...urlErrors.entries()]
+ 395 |     .sort((a, b) => b[1] - a[1])
+     :      ^^^^
+ 396 |     .slice(0, 5)
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/auto-reply/reply/commands-jarvis.ts:59:17]
+ 58 | function formatUptime(startMs: number | null): string {
+ 59 |   if (!startMs) return "ΓÇö";
+    :                 ^^^^^^^^^^^
+ 60 |   const elapsed = Date.now() - startMs;
+    `----
+  help: Replace `return "ΓÇö";` with `{return "ΓÇö";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/auto-reply/reply/commands-jarvis.ts:62:21]
+ 61 |   const seconds = Math.floor(elapsed / 1000);
+ 62 |   if (seconds < 60) return `${seconds}s`;
+    :                     ^^^^^^^^^^^^^^^^^^^^^
+ 63 |   const minutes = Math.floor(seconds / 60);
+    `----
+  help: Replace `return `${seconds}s`;` with `{return `${seconds}s`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/auto-reply/reply/commands-jarvis.ts:64:21]
+ 63 |   const minutes = Math.floor(seconds / 60);
+ 64 |   if (minutes < 60) return `${minutes}m ${seconds % 60}s`;
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 65 |   const hours = Math.floor(minutes / 60);
+    `----
+  help: Replace `return `${minutes}m ${seconds % 60}s`;` with `{return `${minutes}m ${seconds % 60}s`;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security-hardening.ts:47:29]
+ 46 |     const n = Math.floor(port);
+ 47 |     if (n < 0 || n > 65535) throw new SecurityError(`Invalid port: ${port}`);
+    :                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 48 |     return n;
+    `----
+  help: Replace `throw new SecurityError(`Invalid port: ${port}`);` with `{throw new SecurityError(`Invalid port: ${port}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:216:25]
+ 215 |     let results = this.entries;
+ 216 |     if (opts?.category) results = results.filter((e) => e.category === opts.category);
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 217 |     if (opts?.riskLevel) results = results.filter((e) => e.riskLevel === opts.riskLevel);
+     `----
+  help: Replace `results = results.filter((e) => e.category === opts.category);` with `{results = results.filter((e) => e.category === opts.category);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:217:26]
+ 216 |     if (opts?.category) results = results.filter((e) => e.category === opts.category);
+ 217 |     if (opts?.riskLevel) results = results.filter((e) => e.riskLevel === opts.riskLevel);
+     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 218 |     if (opts?.since) results = results.filter((e) => e.timestamp >= opts.since!);
+     `----
+  help: Replace `results = results.filter((e) => e.riskLevel === opts.riskLevel);` with `{results = results.filter((e) => e.riskLevel === opts.riskLevel);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:218:22]
+ 217 |     if (opts?.riskLevel) results = results.filter((e) => e.riskLevel === opts.riskLevel);
+ 218 |     if (opts?.since) results = results.filter((e) => e.timestamp >= opts.since!);
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 219 |     if (opts?.source) results = results.filter((e) => e.source === opts.source);
+     `----
+  help: Replace `results = results.filter((e) => e.timestamp >= opts.since!);` with `{results = results.filter((e) => e.timestamp >= opts.since!);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:219:23]
+ 218 |     if (opts?.since) results = results.filter((e) => e.timestamp >= opts.since!);
+ 219 |     if (opts?.source) results = results.filter((e) => e.source === opts.source);
+     :                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 220 |     if (opts?.limit) results = results.slice(-opts.limit);
+     `----
+  help: Replace `results = results.filter((e) => e.source === opts.source);` with `{results = results.filter((e) => e.source === opts.source);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:220:22]
+ 219 |     if (opts?.source) results = results.filter((e) => e.source === opts.source);
+ 220 |     if (opts?.limit) results = results.slice(-opts.limit);
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 221 |     return results;
+     `----
+  help: Replace `results = results.slice(-opts.limit);` with `{results = results.slice(-opts.limit);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:309:19]
+ 308 |     const session = this.sessions.get(token);
+ 309 |     if (!session) return false;
+     :                   ^^^^^^^^^^^^^
+ 310 | 
+     `----
+  help: Replace `return false;` with `{return false;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security-hardening.ts:345:36]
+ 344 |     for (const [token, session] of this.sessions) {
+ 345 |       if (now > session.expiresAt) this.sessions.delete(token);
+     :                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 346 |     }
+     `----
+  help: Replace `this.sessions.delete(token);` with `{this.sessions.delete(token);}`.
+
+  x eslint(no-unused-vars): Catch parameter 'e' is caught but never used.
+    ,-[src/autonomous/security-hardening.ts:67:14]
+ 66 |       return parsed.href;
+ 67 |     } catch (e) {
+    :              |
+    :              `-- 'e' is declared here
+ 68 |       throw new SecurityError(`Invalid URL: ${url}`);
+    `----
+  help: Consider handling this error.
+
+  x eslint(curly): Expected { after 'for-of'.
+    ,-[src/autonomous/security/wifi-security.ts:55:37]
+ 54 |   private emit(e: WifiEvent): void {
+ 55 |     for (const l of this.listeners) l(e);
+    :                                     ^^^^^
+ 56 |   }
+    `----
+  help: Replace `l(e);` with `{l(e);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/wifi-security.ts:87:13]
+ 86 |     const t = iface ?? this.monitorIface;
+ 87 |     if (!t) return;
+    :             ^^^^^^^
+ 88 |     try {
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:117:27]
+ 116 |       for (const line of stdout.split("\n")) {
+ 117 |         if (!line.trim()) continue;
+     :                           ^^^^^^^^^
+ 118 |         const p = line.split(":");
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:119:28]
+ 118 |         const p = line.split(":");
+ 119 |         if (p.length < 11) continue;
+     :                            ^^^^^^^^^
+ 120 |         const bssid = p.slice(0, 6).join(":");
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:178:25]
+ 177 |     const args = ["--deauth", String(opts.count ?? 5), "-a", opts.bssid];
+ 178 |     if (opts.clientMac) args.push("-c", opts.clientMac);
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 179 |     args.push(opts.iface);
+     `----
+  help: Replace `args.push("-c", opts.clientMac);` with `{args.push("-c", opts.clientMac);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:239:26]
+ 238 |   private enc(s: string): WifiNetwork["encryption"] {
+ 239 |     if (/WPA3/i.test(s)) return "WPA3";
+     :                          ^^^^^^^^^^^^^^
+ 240 |     if (/WPA2/i.test(s)) return "WPA2";
+     `----
+  help: Replace `return "WPA3";` with `{return "WPA3";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:240:26]
+ 239 |     if (/WPA3/i.test(s)) return "WPA3";
+ 240 |     if (/WPA2/i.test(s)) return "WPA2";
+     :                          ^^^^^^^^^^^^^^
+ 241 |     if (/WPA/i.test(s)) return "WPA";
+     `----
+  help: Replace `return "WPA2";` with `{return "WPA2";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:241:25]
+ 240 |     if (/WPA2/i.test(s)) return "WPA2";
+ 241 |     if (/WPA/i.test(s)) return "WPA";
+     :                         ^^^^^^^^^^^^^
+ 242 |     if (/WEP/i.test(s)) return "WEP";
+     `----
+  help: Replace `return "WPA";` with `{return "WPA";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:242:25]
+ 241 |     if (/WPA/i.test(s)) return "WPA";
+ 242 |     if (/WEP/i.test(s)) return "WEP";
+     :                         ^^^^^^^^^^^^^
+ 243 |     if (!s || /^--$/.test(s)) return "Open";
+     `----
+  help: Replace `return "WEP";` with `{return "WEP";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/wifi-security.ts:243:31]
+ 242 |     if (/WEP/i.test(s)) return "WEP";
+ 243 |     if (!s || /^--$/.test(s)) return "Open";
+     :                               ^^^^^^^^^^^^^^
+ 244 |     return "Unknown";
+     `----
+  help: Replace `return "Open";` with `{return "Open";}`.
+
+  x eslint(curly): Expected { after 'for-of'.
+     ,-[src/autonomous/security/web-scanner.ts:199:37]
+ 198 |   private emit(event: ScanEvent): void {
+ 199 |     for (const l of this.listeners) l(event);
+     :                                     ^^^^^^^^^
+ 200 |   }
+     `----
+  help: Replace `l(event);` with `{l(event);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:399:30]
+ 398 | 
+ 399 |     if (params.length === 0) return results;
+     :                              ^^^^^^^^^^^^^^^
+ 400 | 
+     `----
+  help: Replace `return results;` with `{return results;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:584:42]
+ 583 |         const { stdout: headers } = await run("curl", ["-s", "-I", testUrl], { timeout: 10_000 });
+ 584 |         if (/cloudflare/i.test(headers)) waf = "Cloudflare";
+     :                                          ^^^^^^^^^^^^^^^^^^^
+ 585 |         else if (/akamai/i.test(headers)) waf = "Akamai";
+     `----
+  help: Replace `waf = "Cloudflare";` with `{waf = "Cloudflare";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:585:43]
+ 584 |         if (/cloudflare/i.test(headers)) waf = "Cloudflare";
+ 585 |         else if (/akamai/i.test(headers)) waf = "Akamai";
+     :                                           ^^^^^^^^^^^^^^^
+ 586 |         else if (/sucuri/i.test(headers)) waf = "Sucuri";
+     `----
+  help: Replace `waf = "Akamai";` with `{waf = "Akamai";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:586:43]
+ 585 |         else if (/akamai/i.test(headers)) waf = "Akamai";
+ 586 |         else if (/sucuri/i.test(headers)) waf = "Sucuri";
+     :                                           ^^^^^^^^^^^^^^^
+ 587 |         else if (/mod_security/i.test(headers)) waf = "ModSecurity";
+     `----
+  help: Replace `waf = "Sucuri";` with `{waf = "Sucuri";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:587:49]
+ 586 |         else if (/sucuri/i.test(headers)) waf = "Sucuri";
+ 587 |         else if (/mod_security/i.test(headers)) waf = "ModSecurity";
+     :                                                 ^^^^^^^^^^^^^^^^^^^^
+ 588 |         else if (/aws/i.test(headers)) waf = "AWS WAF";
+     `----
+  help: Replace `waf = "ModSecurity";` with `{waf = "ModSecurity";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:588:40]
+ 587 |         else if (/mod_security/i.test(headers)) waf = "ModSecurity";
+ 588 |         else if (/aws/i.test(headers)) waf = "AWS WAF";
+     :                                        ^^^^^^^^^^^^^^^^
+ 589 |         else if (/imperva|incapsula/i.test(headers)) waf = "Imperva/Incapsula";
+     `----
+  help: Replace `waf = "AWS WAF";` with `{waf = "AWS WAF";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:589:54]
+ 588 |         else if (/aws/i.test(headers)) waf = "AWS WAF";
+ 589 |         else if (/imperva|incapsula/i.test(headers)) waf = "Imperva/Incapsula";
+     :                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 590 |         else waf = "Unknown WAF";
+     `----
+  help: Replace `waf = "Imperva/Incapsula";` with `{waf = "Imperva/Incapsula";}`.
+
+  x eslint(curly): Expected { after 'else'.
+     ,-[src/autonomous/security/web-scanner.ts:590:14]
+ 589 |         else if (/imperva|incapsula/i.test(headers)) waf = "Imperva/Incapsula";
+ 590 |         else waf = "Unknown WAF";
+     :              ^^^^^^^^^^^^^^^^^^^^
+ 591 | 
+     `----
+  help: Replace `waf = "Unknown WAF";` with `{waf = "Unknown WAF";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:691:30]
+ 690 |       const args = ["-s", "-I", "--max-time", "10"];
+ 691 |       if (options?.insecure) args.push("-k");
+     :                              ^^^^^^^^^^^^^^^^
+ 692 |       if (options?.userAgent) args.push("-A", options.userAgent);
+     `----
+  help: Replace `args.push("-k");` with `{args.push("-k");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:692:31]
+ 691 |       if (options?.insecure) args.push("-k");
+ 692 |       if (options?.userAgent) args.push("-A", options.userAgent);
+     :                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 693 |       args.push(url);
+     `----
+  help: Replace `args.push("-A", options.userAgent);` with `{args.push("-A", options.userAgent);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:708:28]
+ 707 |     const args = ["-s", "--max-time", "10", "-L"];
+ 708 |     if (options?.insecure) args.push("-k");
+     :                            ^^^^^^^^^^^^^^^^
+ 709 |     if (options?.userAgent) args.push("-A", options.userAgent);
+     `----
+  help: Replace `args.push("-k");` with `{args.push("-k");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:709:29]
+ 708 |     if (options?.insecure) args.push("-k");
+ 709 |     if (options?.userAgent) args.push("-A", options.userAgent);
+     :                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 710 |     if (options?.authToken) args.push("-H", `Authorization: Bearer ${options.authToken}`);
+     `----
+  help: Replace `args.push("-A", options.userAgent);` with `{args.push("-A", options.userAgent);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/web-scanner.ts:710:29]
+ 709 |     if (options?.userAgent) args.push("-A", options.userAgent);
+ 710 |     if (options?.authToken) args.push("-H", `Authorization: Bearer ${options.authToken}`);
+     :                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 711 |     args.push(url);
+     `----
+  help: Replace `args.push("-H", `Authorization: Bearer ${options.authToken}`);` with `{args.push("-H", `Authorization: Bearer ${options.authToken}`);}`.
+
+  x eslint(no-unused-vars): Parameter 'options' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/security/web-scanner.ts:483:32]
+ 482 | 
+ 483 |   async checkCors(url: string, options?: WebScanOptions): Promise<void> {
+     :                                ^^^|^^^
+     :                                   `-- 'options' is declared here
+ 484 |     const origins = [
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(no-unused-vars): Parameter 'options' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/security/web-scanner.ts:515:40]
+ 514 | 
+ 515 |   async checkOpenRedirect(url: string, options?: WebScanOptions): Promise<void> {
+     :                                        ^^^|^^^
+     :                                           `-- 'options' is declared here
+ 516 |     const redirectParams = [
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/playbooks.ts:116:15]
+ 115 |             if (!niktoResult.stdout || niktoResult.stdout.trim() === "")
+ 116 |               throw new Error("Nikto missing");
+     :               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 117 |           } catch (err) {
+     `----
+  help: Replace `throw new Error("Nikto missing");` with `{throw new Error("Nikto missing");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/playbooks.ts:171:17]
+ 170 |               if (!searchResult.stdout || searchResult.stdout.trim() === "")
+ 171 |                 throw new Error("Searchsploit missing");
+     :                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 172 |             } catch (e) {
+     `----
+  help: Replace `throw new Error("Searchsploit missing");` with `{throw new Error("Searchsploit missing");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/playbooks.ts:232:31]
+ 231 |   private truncate(str: string, maxLen: number): string {
+ 232 |     if (str.length <= maxLen) return str;
+     :                               ^^^^^^^^^^^
+ 233 |     return str.substring(0, maxLen) + "\n...[TRUNCATED]...";
+     `----
+  help: Replace `return str;` with `{return str;}`.
+
+  x eslint(no-unused-vars): Catch parameter 'err' is caught but never used.
+    ,-[src/autonomous/security/playbooks.ts:77:16]
+ 76 |         }
+ 77 |       } catch (err) {
+    :                ^|^
+    :                 `-- 'err' is declared here
+ 78 |         // Fallback mock if nmap is missing on host
+    `----
+  help: Consider handling this error.
+
+  x eslint(no-unused-vars): Catch parameter 'err' is caught but never used.
+     ,-[src/autonomous/security/playbooks.ts:117:20]
+ 116 |               throw new Error("Nikto missing");
+ 117 |           } catch (err) {
+     :                    ^|^
+     :                     `-- 'err' is declared here
+ 118 |             niktoResult.stdout = `+ Server: Apache/2.4.41
+     `----
+  help: Consider handling this error.
+
+  x eslint(no-unused-vars): Catch parameter 'e' is caught but never used.
+     ,-[src/autonomous/security/playbooks.ts:141:22]
+ 140 |               sqlResult = await WebAttacks.sqlmap(niktoUrl, "--batch", "--random-agent");
+ 141 |             } catch (e) {} // Fallback to sqlResult.stdout above
+     :                      |
+     :                      `-- 'e' is declared here
+ 142 |             report.vulnerabilities.push({
+     `----
+  help: Consider handling this error.
+
+  x eslint(no-unused-vars): Catch parameter 'e' is caught but never used.
+     ,-[src/autonomous/security/playbooks.ts:172:22]
+ 171 |                 throw new Error("Searchsploit missing");
+ 172 |             } catch (e) {
+     :                      |
+     :                      `-- 'e' is declared here
+ 173 |               searchResult.stdout = `Exploits:
+     `----
+  help: Consider handling this error.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/security/playbooks.ts:15:10]
+ 14 |   message: string;
+ 15 |   data?: any;
+    :          ^^^
+ 16 | }
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/security/playbooks.ts:23:20]
+ 22 |   openPorts: number[];
+ 23 |   vulnerabilities: any[];
+    :                    ^^^
+ 24 |   exploitsFound: any[];
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/security/playbooks.ts:24:18]
+ 23 |   vulnerabilities: any[];
+ 24 |   exploitsFound: any[];
+    :                  ^^^
+ 25 |   markdownReport: string;
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/security/playbooks.ts:41:12]
+ 40 |     message: string,
+ 41 |     data?: any,
+    :            ^^^
+ 42 |   ) {
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+    ,-[src/autonomous/security/playbooks.ts:71:23]
+ 70 |       this.emit("recon", "started", `Initiating Nmap service scan against ${this.target}...`);
+ 71 |       let nmapResult: any = { stdout: "" };
+    :                       ^^^
+ 72 |       try {
+    `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/security/playbooks.ts:111:28]
+ 110 | 
+ 111 |           let niktoResult: any = { stdout: "" };
+     :                            ^^^
+ 112 |           let sqlResult: any = { stdout: "" };
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/security/playbooks.ts:112:26]
+ 111 |           let niktoResult: any = { stdout: "" };
+ 112 |           let sqlResult: any = { stdout: "" };
+     :                          ^^^
+ 113 |           try {
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/security/playbooks.ts:167:31]
+ 166 |           if (searchQuery.length > 2) {
+ 167 |             let searchResult: any = { exitCode: 0, stdout: "" };
+     :                               ^^^
+ 168 |             try {
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'for-of'.
+    ,-[src/autonomous/security/packet-sniffer.ts:53:37]
+ 52 |   private emit(e: SnifferEvent): void {
+ 53 |     for (const l of this.listeners) l(e);
+    :                                     ^^^^^
+ 54 |   }
+    `----
+  help: Replace `l(e);` with `{l(e);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/packet-sniffer.ts:75:25]
+ 74 |     const args = ["-i", iface, "-w", outFile, "-l"];
+ 75 |     if (options.filter) args.push(options.filter);
+    :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 76 |     if (options.count) args.push("-c", String(options.count));
+    `----
+  help: Replace `args.push(options.filter);` with `{args.push(options.filter);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/packet-sniffer.ts:76:24]
+ 75 |     if (options.filter) args.push(options.filter);
+ 76 |     if (options.count) args.push("-c", String(options.count));
+    :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 77 |     if (!options.promiscuous) args.push("-p");
+    `----
+  help: Replace `args.push("-c", String(options.count));` with `{args.push("-c", String(options.count));}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/packet-sniffer.ts:77:31]
+ 76 |     if (options.count) args.push("-c", String(options.count));
+ 77 |     if (!options.promiscuous) args.push("-p");
+    :                               ^^^^^^^^^^^^^^^^
+ 78 | 
+    `----
+  help: Replace `args.push("-p");` with `{args.push("-p");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/packet-sniffer.ts:84:17]
+ 83 |       const line = data.toString().trim();
+ 84 |       if (line) this.emit({ kind: "status", message: line });
+    :                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 85 |     });
+    `----
+  help: Replace `this.emit({ kind: "status", message: line });` with `{this.emit({ kind: "status", message: line });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:125:17]
+ 124 |     ];
+ 125 |     if (filter) args.push("-Y", filter);
+     :                 ^^^^^^^^^^^^^^^^^^^^^^^^
+ 126 | 
+     `----
+  help: Replace `args.push("-Y", filter);` with `{args.push("-Y", filter);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:130:27]
+ 129 |       for (const line of stdout.split("\n")) {
+ 130 |         if (!line.trim()) continue;
+     :                           ^^^^^^^^^
+ 131 |         const [ts, src, dst, proto, len, info] = line.split("|");
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:144:19]
+ 143 |       const tdArgs = ["-r", pcapFile, "-nn", "-c", String(maxPackets)];
+ 144 |       if (filter) tdArgs.push(filter);
+     :                   ^^^^^^^^^^^^^^^^^^^^
+ 145 |       try {
+     `----
+  help: Replace `tdArgs.push(filter);` with `{tdArgs.push(filter);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:148:29]
+ 147 |         for (const line of stdout.split("\n")) {
+ 148 |           if (!line.trim()) continue;
+     :                             ^^^^^^^^^
+ 149 |           const m = line.match(/^(\S+)\s+IP\s+(\S+)\s+>\s+(\S+?):\s+(.+)/);
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:151:13]
+ 150 |               if (m)
+ 151 | ,->             packets.push({
+ 152 | |                 timestamp: m[1],
+ 153 | |                 src: m[2],
+ 154 | |                 dst: m[3],
+ 155 | |                 protocol: "TCP/IP",
+ 156 | |                 length: 0,
+ 157 | |                 info: m[4],
+ 158 | `->             });
+ 159 |             }
+     `----
+  help: Replace `packets.push({
+                      timestamp: m[1],
+                      src: m[2],
+                      dst: m[3],
+                      protocol: "TCP/IP",
+                      length: 0,
+                      info: m[4],
+                    });` with `{packets.push({
+                      timestamp: m[1],
+                      src: m[2],
+                      dst: m[3],
+                      protocol: "TCP/IP",
+                      length: 0,
+                      info: m[4],
+                    });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:174:18]
+ 173 |       protocols[p.protocol] = (protocols[p.protocol] ?? 0) + 1;
+ 174 |       if (p.src) sources[p.src] = (sources[p.src] ?? 0) + 1;
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 175 |       if (p.dst) dests[p.dst] = (dests[p.dst] ?? 0) + 1;
+     `----
+  help: Replace `sources[p.src] = (sources[p.src] ?? 0) + 1;` with `{sources[p.src] = (sources[p.src] ?? 0) + 1;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:175:18]
+ 174 |       if (p.src) sources[p.src] = (sources[p.src] ?? 0) + 1;
+ 175 |       if (p.dst) dests[p.dst] = (dests[p.dst] ?? 0) + 1;
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 176 |     }
+     `----
+  help: Replace `dests[p.dst] = (dests[p.dst] ?? 0) + 1;` with `{dests[p.dst] = (dests[p.dst] ?? 0) + 1;}`.
+
+  x eslint(curly): Expected { after 'for-of'.
+     ,-[src/autonomous/security/packet-sniffer.ts:203:41]
+ 202 |       );
+ 203 |       for (const l of http.split("\n")) if (l.trim()) creds.push(`HTTP Basic: ${l.trim()}`);
+     :                                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 204 |       // FTP
+     `----
+  help: Replace `if (l.trim()) creds.push(`HTTP Basic: ${l.trim()}`);` with `{if (l.trim()) creds.push(`HTTP Basic: ${l.trim()}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:203:55]
+ 202 |       );
+ 203 |       for (const l of http.split("\n")) if (l.trim()) creds.push(`HTTP Basic: ${l.trim()}`);
+     :                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 204 |       // FTP
+     `----
+  help: Replace `creds.push(`HTTP Basic: ${l.trim()}`);` with `{creds.push(`HTTP Basic: ${l.trim()}`);}`.
+
+  x eslint(curly): Expected { after 'for-of'.
+     ,-[src/autonomous/security/packet-sniffer.ts:221:40]
+ 220 |       );
+ 221 |       for (const l of ftp.split("\n")) if (l.trim()) creds.push(`FTP: ${l.trim()}`);
+     :                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 222 |     } catch {}
+     `----
+  help: Replace `if (l.trim()) creds.push(`FTP: ${l.trim()}`);` with `{if (l.trim()) creds.push(`FTP: ${l.trim()}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/packet-sniffer.ts:221:54]
+ 220 |       );
+ 221 |       for (const l of ftp.split("\n")) if (l.trim()) creds.push(`FTP: ${l.trim()}`);
+     :                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 222 |     } catch {}
+     `----
+  help: Replace `creds.push(`FTP: ${l.trim()}`);` with `{creds.push(`FTP: ${l.trim()}`);}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/security/packet-sniffer.ts:180:10]
+ 179 |       Object.entries(obj)
+ 180 |         .sort((a, b) => b[1] - a[1])
+     :          ^^^^
+ 181 |         .slice(0, n);
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'for-of'.
+    ,-[src/autonomous/security/network-recon.ts:93:37]
+ 92 |   private emit(event: ReconEvent): void {
+ 93 |     for (const l of this.listeners) l(event);
+    :                                     ^^^^^^^^^
+ 94 |   }
+    `----
+  help: Replace `l(event);` with `{l(event);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:145:16]
+ 144 |     const args = ["-l"];
+ 145 |     if (iface) args.push("-I", iface);
+     :                ^^^^^^^^^^^^^^^^^^^^^^^
+ 146 |     try {
+     `----
+  help: Replace `args.push("-I", iface);` with `{args.push("-I", iface);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:186:14]
+ 185 |       const m = hostnames.stdout.match(/pointer\s+(.+)\./);
+ 186 |       if (m) result.hostname = m[1];
+     :              ^^^^^^^^^^^^^^^^^^^^^^^
+ 187 |     } catch {}
+     `----
+  help: Replace `result.hostname = m[1];` with `{result.hostname = m[1];}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:214:35]
+ 213 | 
+ 214 |     if (options.serviceDetection) args.push("-sV");
+     :                                   ^^^^^^^^^^^^^^^^^
+ 215 |     if (options.osDetection) args.push("-O");
+     `----
+  help: Replace `args.push("-sV");` with `{args.push("-sV");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:215:30]
+ 214 |     if (options.serviceDetection) args.push("-sV");
+ 215 |     if (options.osDetection) args.push("-O");
+     :                              ^^^^^^^^^^^^^^^^
+ 216 |     if (options.aggressive) args.push("-T4");
+     `----
+  help: Replace `args.push("-O");` with `{args.push("-O");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:216:29]
+ 215 |     if (options.osDetection) args.push("-O");
+ 216 |     if (options.aggressive) args.push("-T4");
+     :                             ^^^^^^^^^^^^^^^^^
+ 217 |     if (options.ports) args.push("-p", options.ports);
+     `----
+  help: Replace `args.push("-T4");` with `{args.push("-T4");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:217:24]
+ 216 |     if (options.aggressive) args.push("-T4");
+ 217 |     if (options.ports) args.push("-p", options.ports);
+     :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 218 | 
+     `----
+  help: Replace `args.push("-p", options.ports);` with `{args.push("-p", options.ports);}`.
+
+  x eslint(curly): Expected { after 'for' condition.
+     ,-[src/autonomous/security/network-recon.ts:468:61]
+ 467 |         const [start, end] = trimmed.split("-").map(Number);
+ 468 |         for (let i = start; i <= Math.min(end, 65535); i++) ports.push(i);
+     :                                                             ^^^^^^^^^^^^^^
+ 469 |       } else {
+     `----
+  help: Replace `ports.push(i);` with `{ports.push(i);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/network-recon.ts:471:34]
+ 470 |         const p = parseInt(trimmed);
+ 471 |         if (p > 0 && p <= 65535) ports.push(p);
+     :                                  ^^^^^^^^^^^^^^
+ 472 |       }
+     `----
+  help: Replace `ports.push(p);` with `{ports.push(p);}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/security/network-recon.ts:299:18]
+ 298 | 
+ 299 |     return ports.sort((a, b) => a.port - b.port);
+     :                  ^^^^
+ 300 |   }
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(no-unused-vars): Parameter 'hash' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/security/kali-tools.ts:135:20]
+ 134 |   rainbowcrack: (hash: string) => exec("rcrack", [".", "-h", hash]),
+ 135 |   hashIdentifier: (hash: string) => exec("hash-identifier", [], 5000),
+     :                    ^^|^
+     :                      `-- 'hash' is declared here
+ 136 |   patator: (module: string, ...args: string[]) => exec("patator", [module, ...args]),
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(no-unused-vars): Parameter 'rhost' is declared but never used. Unused parameters should start with a '_'.
+     ,-[src/autonomous/security/kali-tools.ts:207:44]
+ 206 |     exec("powershell", ["-c", `Import-Module PowerSploit; ${module}`]),
+ 207 |   pivoting: (lhost: string, lport: string, rhost: string) =>
+     :                                            ^^|^^
+     :                                              `-- 'rhost' is declared here
+ 208 |     exec("chisel", ["server", "-p", lport, "--reverse"]),
+     `----
+  help: Consider removing this parameter.
+
+  x eslint(curly): Expected { after 'for-of'.
+    ,-[src/autonomous/security/exploit-engine.ts:48:37]
+ 47 |   private emit(e: ExploitEvent): void {
+ 48 |     for (const l of this.listeners) l(e);
+    :                                     ^^^^^
+ 49 |   }
+    `----
+  help: Replace `l(e);` with `{l(e);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/exploit-engine.ts:152:25]
+ 151 |     ];
+ 152 |     if (config.encoder) args.push("-e", config.encoder);
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 153 |     const { stdout } = await run("msfvenom", args, { timeout: 60_000 });
+     `----
+  help: Replace `args.push("-e", config.encoder);` with `{args.push("-e", config.encoder);}`.
+
+  x eslint(no-unused-vars): Identifier 'spawn' is imported but never used.
+   ,-[src/autonomous/security/exploit-engine.ts:6:20]
+ 5 | 
+ 6 | import { execFile, spawn } from "node:child_process";
+   :                    ^^|^^
+   :                      `-- 'spawn' is imported here
+ 7 | import { promisify } from "node:util";
+   `----
+  help: Consider removing this import.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:51:21]
+ 50 |     const candidates: string[] = [];
+ 51 |     if (len === 32) candidates.push("MD5", "NTLM");
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 52 |     if (len === 40) candidates.push("SHA-1");
+    `----
+  help: Replace `candidates.push("MD5", "NTLM");` with `{candidates.push("MD5", "NTLM");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:52:21]
+ 51 |     if (len === 32) candidates.push("MD5", "NTLM");
+ 52 |     if (len === 40) candidates.push("SHA-1");
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^
+ 53 |     if (len === 56) candidates.push("SHA-224");
+    `----
+  help: Replace `candidates.push("SHA-1");` with `{candidates.push("SHA-1");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:53:21]
+ 52 |     if (len === 40) candidates.push("SHA-1");
+ 53 |     if (len === 56) candidates.push("SHA-224");
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 54 |     if (len === 64) candidates.push("SHA-256", "SHA3-256", "BLAKE2s");
+    `----
+  help: Replace `candidates.push("SHA-224");` with `{candidates.push("SHA-224");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:54:21]
+ 53 |     if (len === 56) candidates.push("SHA-224");
+ 54 |     if (len === 64) candidates.push("SHA-256", "SHA3-256", "BLAKE2s");
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 55 |     if (len === 96) candidates.push("SHA-384");
+    `----
+  help: Replace `candidates.push("SHA-256", "SHA3-256", "BLAKE2s");` with `{candidates.push("SHA-256", "SHA3-256", "BLAKE2s");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:55:21]
+ 54 |     if (len === 64) candidates.push("SHA-256", "SHA3-256", "BLAKE2s");
+ 55 |     if (len === 96) candidates.push("SHA-384");
+    :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 56 |     if (len === 128) candidates.push("SHA-512", "SHA3-512", "BLAKE2b");
+    `----
+  help: Replace `candidates.push("SHA-384");` with `{candidates.push("SHA-384");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:56:22]
+ 55 |     if (len === 96) candidates.push("SHA-384");
+ 56 |     if (len === 128) candidates.push("SHA-512", "SHA3-512", "BLAKE2b");
+    :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 57 |     if (hash.startsWith("$2")) candidates.push("bcrypt");
+    `----
+  help: Replace `candidates.push("SHA-512", "SHA3-512", "BLAKE2b");` with `{candidates.push("SHA-512", "SHA3-512", "BLAKE2b");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:57:32]
+ 56 |     if (len === 128) candidates.push("SHA-512", "SHA3-512", "BLAKE2b");
+ 57 |     if (hash.startsWith("$2")) candidates.push("bcrypt");
+    :                                ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 58 |     if (hash.startsWith("$6$")) candidates.push("SHA-512 crypt");
+    `----
+  help: Replace `candidates.push("bcrypt");` with `{candidates.push("bcrypt");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:58:33]
+ 57 |     if (hash.startsWith("$2")) candidates.push("bcrypt");
+ 58 |     if (hash.startsWith("$6$")) candidates.push("SHA-512 crypt");
+    :                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 59 |     if (hash.startsWith("$5$")) candidates.push("SHA-256 crypt");
+    `----
+  help: Replace `candidates.push("SHA-512 crypt");` with `{candidates.push("SHA-512 crypt");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:59:33]
+ 58 |     if (hash.startsWith("$6$")) candidates.push("SHA-512 crypt");
+ 59 |     if (hash.startsWith("$5$")) candidates.push("SHA-256 crypt");
+    :                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 60 |     if (hash.startsWith("$1$")) candidates.push("MD5 crypt");
+    `----
+  help: Replace `candidates.push("SHA-256 crypt");` with `{candidates.push("SHA-256 crypt");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:60:33]
+ 59 |     if (hash.startsWith("$5$")) candidates.push("SHA-256 crypt");
+ 60 |     if (hash.startsWith("$1$")) candidates.push("MD5 crypt");
+    :                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 61 |     if (hash.startsWith("$apr1$")) candidates.push("Apache MD5");
+    `----
+  help: Replace `candidates.push("MD5 crypt");` with `{candidates.push("MD5 crypt");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:61:36]
+ 60 |     if (hash.startsWith("$1$")) candidates.push("MD5 crypt");
+ 61 |     if (hash.startsWith("$apr1$")) candidates.push("Apache MD5");
+    :                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 62 |     if (hash.startsWith("$argon2")) candidates.push("Argon2");
+    `----
+  help: Replace `candidates.push("Apache MD5");` with `{candidates.push("Apache MD5");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:62:37]
+ 61 |     if (hash.startsWith("$apr1$")) candidates.push("Apache MD5");
+ 62 |     if (hash.startsWith("$argon2")) candidates.push("Argon2");
+    :                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 63 |     return candidates.length ? candidates : ["Unknown"];
+    `----
+  help: Replace `candidates.push("Argon2");` with `{candidates.push("Argon2");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/security/crypto-toolkit.ts:95:16]
+ 94 |         const m = line.match(/^([^:]+):(.+)/);
+ 95 |         if (m) results.push({ cracked: true, hash: m[1], plaintext: m[2] });
+    :                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 96 |       }
+    `----
+  help: Replace `results.push({ cracked: true, hash: m[1], plaintext: m[2] });` with `{results.push({ cracked: true, hash: m[1], plaintext: m[2] });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/crypto-toolkit.ts:137:20]
+ 136 |     let charset = "";
+ 137 |     if (uppercase) charset += "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 138 |     if (lowercase) charset += "abcdefghijklmnopqrstuvwxyz";
+     `----
+  help: Replace `charset += "ABCDEFGHIJKLMNOPQRSTUVWXYZ";` with `{charset += "ABCDEFGHIJKLMNOPQRSTUVWXYZ";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/crypto-toolkit.ts:138:20]
+ 137 |     if (uppercase) charset += "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
+ 138 |     if (lowercase) charset += "abcdefghijklmnopqrstuvwxyz";
+     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 139 |     if (digits) charset += "0123456789";
+     `----
+  help: Replace `charset += "abcdefghijklmnopqrstuvwxyz";` with `{charset += "abcdefghijklmnopqrstuvwxyz";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/crypto-toolkit.ts:139:17]
+ 138 |     if (lowercase) charset += "abcdefghijklmnopqrstuvwxyz";
+ 139 |     if (digits) charset += "0123456789";
+     :                 ^^^^^^^^^^^^^^^^^^^^^^^^
+ 140 |     if (symbols) charset += "!@#$%^&*()_+-=[]{}|;:,.<>?";
+     `----
+  help: Replace `charset += "0123456789";` with `{charset += "0123456789";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/crypto-toolkit.ts:140:18]
+ 139 |     if (digits) charset += "0123456789";
+ 140 |     if (symbols) charset += "!@#$%^&*()_+-=[]{}|;:,.<>?";
+     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 141 |     if (!charset) charset = "abcdefghijklmnopqrstuvwxyz0123456789";
+     `----
+  help: Replace `charset += "!@#$%^&*()_+-=[]{}|;:,.<>?";` with `{charset += "!@#$%^&*()_+-=[]{}|;:,.<>?";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/security/crypto-toolkit.ts:141:19]
+ 140 |     if (symbols) charset += "!@#$%^&*()_+-=[]{}|;:,.<>?";
+ 141 |     if (!charset) charset = "abcdefghijklmnopqrstuvwxyz0123456789";
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 142 |     const passwords: string[] = [];
+     `----
+  help: Replace `charset = "abcdefghijklmnopqrstuvwxyz0123456789";` with `{charset = "abcdefghijklmnopqrstuvwxyz0123456789";}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/screen-ocr.ts:97:43]
+ 96 |   async isAvailable(): Promise<boolean> {
+ 97 |     if (this.tesseractAvailable !== null) return this.tesseractAvailable;
+    :                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 98 |     try {
+    `----
+  help: Replace `return this.tesseractAvailable;` with `{return this.tesseractAvailable;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/screen-ocr.ts:288:26]
+ 287 |         const num = parseFloat(m.replace(/,/g, ""));
+ 288 |         if (!isNaN(num)) numbers.push(num);
+     :                          ^^^^^^^^^^^^^^^^^^
+ 289 |       }
+     `----
+  help: Replace `numbers.push(num);` with `{numbers.push(num);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/screen-ocr.ts:304:29]
+ 303 |       const cols = line.split("\t");
+ 304 |       if (cols.length < 12) continue;
+     :                             ^^^^^^^^^
+ 305 | 
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint(curly): Expected { after 'for' condition.
+    ,-[src/autonomous/scheduler.ts:37:40]
+ 36 |     if (trimmed === "*") {
+ 37 |       for (let i = min; i <= max; i++) values.add(i);
+    :                                        ^^^^^^^^^^^^^^
+ 38 |       continue;
+    `----
+  help: Replace `values.add(i);` with `{values.add(i);}`.
+
+  x eslint(curly): Expected { after 'for' condition.
+    ,-[src/autonomous/scheduler.ts:46:48]
+ 45 |       const step = Number(stepMatch[4]);
+ 46 |       for (let i = start; i <= end; i += step) values.add(i);
+    :                                                ^^^^^^^^^^^^^^
+ 47 |       continue;
+    `----
+  help: Replace `values.add(i);` with `{values.add(i);}`.
+
+  x eslint(curly): Expected { after 'for' condition.
+    ,-[src/autonomous/scheduler.ts:54:42]
+ 53 |       const end = Number(rangeMatch[2]);
+ 54 |       for (let i = start; i <= end; i++) values.add(i);
+    :                                          ^^^^^^^^^^^^^^
+ 55 |       continue;
+    `----
+  help: Replace `values.add(i);` with `{values.add(i);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/scheduler.ts:175:21]
+ 174 |   start(): void {
+ 175 |     if (this.timer) return;
+     :                     ^^^^^^^
+ 176 |     this.timer = setInterval(() => this.tick(), 60_000);
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/scheduler.ts:191:26]
+ 190 |   tick(): void {
+ 191 |     if (!this.goalQueue) return;
+     :                          ^^^^^^^
+ 192 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/scheduler.ts:196:27]
+ 195 |     for (const sched of this.schedules.values()) {
+ 196 |       if (!sched.enabled) continue;
+     :                           ^^^^^^^^^
+ 197 | 
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+    ,-[src/autonomous/scheduler.ts:64:29]
+ 63 | 
+ 64 |   return Array.from(values).sort((a, b) => a - b);
+    :                             ^^^^
+ 65 | }
+    `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:258:19]
+ 257 |     const command = body.command as string;
+ 258 |     if (!command) return this.json(res, 400, { error: "Missing 'command' field" });
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 259 |     if (!this.commander) return this.json(res, 503, { error: "NL Commander not initialized" });
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing 'command' field" });` with `{return this.json(res, 400, { error: "Missing 'command' field" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:259:26]
+ 258 |     if (!command) return this.json(res, 400, { error: "Missing 'command' field" });
+ 259 |     if (!this.commander) return this.json(res, 503, { error: "NL Commander not initialized" });
+     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 260 | 
+     `----
+  help: Replace `return this.json(res, 503, { error: "NL Commander not initialized" });` with `{return this.json(res, 503, { error: "NL Commander not initialized" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:273:23]
+ 272 |     const description = body.description as string;
+ 273 |     if (!description) return this.json(res, 400, { error: "Missing 'description'" });
+     :                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 274 |     // TODO: wire to goal queue
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing 'description'" });` with `{return this.json(res, 400, { error: "Missing 'description'" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:279:27]
+ 278 |   private async handleScreenshot(res: ServerResponse): Promise<void> {
+ 279 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 280 |     const screenshot = await this.controller.captureScreen();
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:286:27]
+ 285 |   private async handleListWindows(res: ServerResponse): Promise<void> {
+ 286 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 287 |     const windows = await this.controller.listWindows();
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:292:27]
+ 291 |   private async handleClick(body: Record<string, unknown>, res: ServerResponse): Promise<void> {
+ 292 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 293 |     const x = body.x as number;
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:295:45]
+ 294 |     const y = body.y as number;
+ 295 |     if (x === undefined || y === undefined) return this.json(res, 400, { error: "Missing x, y" });
+     :                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 296 |     await this.controller.click(x, y, body.button as any);
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing x, y" });` with `{return this.json(res, 400, { error: "Missing x, y" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:301:27]
+ 300 |   private async handleType(body: Record<string, unknown>, res: ServerResponse): Promise<void> {
+ 301 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 302 |     const text = body.text as string;
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:303:16]
+ 302 |     const text = body.text as string;
+ 303 |     if (!text) return this.json(res, 400, { error: "Missing 'text'" });
+     :                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 304 |     await this.controller.type(text);
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing 'text'" });` with `{return this.json(res, 400, { error: "Missing 'text'" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:309:27]
+ 308 |   private async handleHotkey(body: Record<string, unknown>, res: ServerResponse): Promise<void> {
+ 309 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 310 |     const modifiers = body.modifiers as string[];
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:312:29]
+ 311 |     const key = body.key as string;
+ 312 |     if (!modifiers || !key) return this.json(res, 400, { error: "Missing modifiers, key" });
+     :                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 313 |     await this.controller.hotkey(modifiers as any, key);
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing modifiers, key" });` with `{return this.json(res, 400, { error: "Missing modifiers, key" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:318:27]
+ 317 |   private async handleOpen(body: Record<string, unknown>, res: ServerResponse): Promise<void> {
+ 318 |     if (!this.controller) return this.json(res, 503, { error: "Controller not ready" });
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 319 |     if (body.app) {
+     `----
+  help: Replace `return this.json(res, 503, { error: "Controller not ready" });` with `{return this.json(res, 503, { error: "Controller not ready" });}`.
+
+  x eslint(curly): Expected { after 'else'.
+     ,-[src/autonomous/rest-api.ts:325:12]
+ 324 |       await this.controller.openFile(body.file as string);
+ 325 |     } else return this.json(res, 400, { error: "Provide app, url, or file" });
+     :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 326 |     this.json(res, 200, { status: "opened" });
+     `----
+  help: Replace `return this.json(res, 400, { error: "Provide app, url, or file" });` with `{return this.json(res, 400, { error: "Provide app, url, or file" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:331:7]
+ 330 |     if (!this.controller?.system)
+ 331 |       return this.json(res, 503, { error: "System controller not available" });
+     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 332 |     const command = body.command as string;
+     `----
+  help: Replace `return this.json(res, 503, { error: "System controller not available" });` with `{return this.json(res, 503, { error: "System controller not available" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:333:19]
+ 332 |     const command = body.command as string;
+ 333 |     if (!command) return this.json(res, 400, { error: "Missing 'command'" });
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 334 | 
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing 'command'" });` with `{return this.json(res, 400, { error: "Missing 'command'" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:362:25]
+ 361 |   private handleApproval(body: Record<string, unknown>, res: ServerResponse): void {
+ 362 |     if (!this.approval) return this.json(res, 503, { error: "Approval gate not available" });
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 363 |     const requestId = body.requestId as string;
+     `----
+  help: Replace `return this.json(res, 503, { error: "Approval gate not available" });` with `{return this.json(res, 503, { error: "Approval gate not available" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:366:7]
+ 365 |     if (!requestId || approved === undefined)
+ 366 |       return this.json(res, 400, { error: "Missing requestId, approved" });
+     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 367 |     this.approval.resolve(requestId, approved, body.alwaysAllow as boolean);
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing requestId, approved" });` with `{return this.json(res, 400, { error: "Missing requestId, approved" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:372:24]
+ 371 |   private handleGetProfile(res: ServerResponse): void {
+ 372 |     if (!this.profile) return this.json(res, 503, { error: "Profile not available" });
+     :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 373 |     this.json(res, 200, this.profile.getData());
+     `----
+  help: Replace `return this.json(res, 503, { error: "Profile not available" });` with `{return this.json(res, 503, { error: "Profile not available" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:377:26]
+ 376 |   private handleListWorkflows(res: ServerResponse): void {
+ 377 |     if (!this.workflows) return this.json(res, 503, { error: "Workflows not available" });
+     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 378 |     this.json(res, 200, { workflows: this.workflows.list() });
+     `----
+  help: Replace `return this.json(res, 503, { error: "Workflows not available" });` with `{return this.json(res, 503, { error: "Workflows not available" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:385:26]
+ 384 |   ): Promise<void> {
+ 385 |     if (!this.workflows) return this.json(res, 503, { error: "Workflows not available" });
+     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 386 |     const id = body.workflowId as string;
+     `----
+  help: Replace `return this.json(res, 503, { error: "Workflows not available" });` with `{return this.json(res, 503, { error: "Workflows not available" });}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/rest-api.ts:387:14]
+ 386 |     const id = body.workflowId as string;
+ 387 |     if (!id) return this.json(res, 400, { error: "Missing workflowId" });
+     :              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 388 |     const result = await this.workflows.replay(id);
+     `----
+  help: Replace `return this.json(res, 400, { error: "Missing workflowId" });` with `{return this.json(res, 400, { error: "Missing workflowId" });}`.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/rest-api.ts:296:54]
+ 295 |     if (x === undefined || y === undefined) return this.json(res, 400, { error: "Missing x, y" });
+ 296 |     await this.controller.click(x, y, body.button as any);
+     :                                                      ^^^
+ 297 |     this.json(res, 200, { status: "clicked", x, y });
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/rest-api.ts:313:47]
+ 312 |     if (!modifiers || !key) return this.json(res, 400, { error: "Missing modifiers, key" });
+ 313 |     await this.controller.hotkey(modifiers as any, key);
+     :                                               ^^^
+ 314 |     this.json(res, 200, { status: "pressed", combo: `${modifiers.join("+")}+${key}` });
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(no-unused-vars): Type 'Action' is imported but never used.
+   ,-[src/autonomous/repair.ts:8:15]
+ 7 | 
+ 8 | import type { Action, ErrorType, Goal, RepairDiagnosis, VisionAnalysis } from "./types.js";
+   :               ^^^|^^
+   :                  `-- 'Action' is imported here
+ 9 | 
+   `----
+  help: Consider removing this import.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/recorder.ts:82:32]
+ 81 |   recordStep(action: Action, meta?: { url?: string; title?: string }): void {
+ 82 |     if (!this.activeRecording) return;
+    :                                ^^^^^^^
+ 83 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/recorder.ts:102:32]
+ 101 |   async finishRecording(success: boolean): Promise<Recording | null> {
+ 102 |     if (!this.activeRecording) return null;
+     :                                ^^^^^^^^^^^^
+ 103 | 
+     `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/recorder.ts:158:38]
+ 157 |       for (const file of files) {
+ 158 |         if (!file.endsWith(".json")) continue;
+     :                                      ^^^^^^^^^
+ 159 |         try {
+     `----
+  help: Replace `continue;` with `{continue;}`.
+
+  x eslint-plugin-unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
+     ,-[src/autonomous/recorder.ts:174:24]
+ 173 | 
+ 174 |       return summaries.sort((a, b) => b.createdAt.localeCompare(a.createdAt));
+     :                        ^^^^
+ 175 |     } catch {
+     `----
+  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/plugins.ts:86:18]
+ 85 |     const plugin = this.plugins.get(name);
+ 86 |     if (!plugin) return;
+    :                  ^^^^^^^
+ 87 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/plugins.ts:192:34]
+ 191 |   generateToolPrompt(): string {
+ 192 |     if (this.plugins.size === 0) return "";
+     :                                  ^^^^^^^^^^
+ 193 | 
+     `----
+  help: Replace `return "";` with `{return "";}`.
+
+  x eslint(no-unused-vars): Type 'Action' is imported but never used.
+    ,-[src/autonomous/plugins.ts:12:15]
+ 11 | import { join, extname } from "node:path";
+ 12 | import type { Action, Goal } from "./types.js";
+    :               ^^^|^^
+    :                  `-- 'Action' is imported here
+ 13 | 
+    `----
+  help: Consider removing this import.
+
+  x eslint-plugin-unicorn(no-useless-fallback-in-spread): Empty fallbacks in spreads are unnecessary
+     ,-[src/autonomous/plugins.ts:253:13]
+ 252 |             "Content-Type": "application/json",
+ 253 |             ...((params.headers as Record<string, string>) ?? {}),
+     :             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 254 |           };
+     `----
+  help: Spreading falsy values in object literals won't add any unexpected properties, so it's unnecessary to add an empty object as fallback.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/nl-commander.ts:364:46]
+ 363 |             action: {
+ 364 |               action: actionMap[action!]! as any,
+     :                                              ^^^
+ 365 |               windowTitle: target?.trim(),
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/nl-commander.ts:417:34]
+ 416 |               action: "device_hotkey",
+ 417 |               modifiers: [mod as any],
+     :                                  ^^^
+ 418 |               key,
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/nl-commander.ts:511:18]
+ 510 |               reasoning: "Booting AutoPentester",
+ 511 |             } as any,
+     :                  ^^^
+ 512 |             waitAfterMs: 1000,
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/multi-monitor.ts:43:18]
+ 42 |     execFile(cmd, args, { timeout: 5000 }, (error, stdout) => {
+ 43 |       if (error) reject(new Error(error.message));
+    :                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 44 |       else resolve(stdout);
+    `----
+  help: Replace `reject(new Error(error.message));` with `{reject(new Error(error.message));}`.
+
+  x eslint(curly): Expected { after 'else'.
+    ,-[src/autonomous/multi-monitor.ts:44:12]
+ 43 |       if (error) reject(new Error(error.message));
+ 44 |       else resolve(stdout);
+    :            ^^^^^^^^^^^^^^^^
+ 45 |     });
+    `----
+  help: Replace `resolve(stdout);` with `{resolve(stdout);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/multi-monitor.ts:91:19]
+ 90 |     const monitor = screen.monitors.find((m) => m.name === monitorName);
+ 91 |     if (!monitor) throw new Error(`Monitor not found: ${monitorName}`);
+    :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 92 |     return { x: monitor.x + localX, y: monitor.y + localY };
+    `----
+  help: Replace `throw new Error(`Monitor not found: ${monitorName}`);` with `{throw new Error(`Monitor not found: ${monitorName}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/multi-monitor.ts:99:19]
+  98 |     const monitor = screen.monitors.find((m) => m.name === monitorName);
+  99 |     if (!monitor) throw new Error(`Monitor not found: ${monitorName}`);
+     :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 100 | 
+     `----
+  help: Replace `throw new Error(`Monitor not found: ${monitorName}`);` with `{throw new Error(`Monitor not found: ${monitorName}`);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/message-queue.ts:50:21]
+ 49 |     const idx = this.queue.findIndex((m) => !m.processed);
+ 50 |     if (idx === -1) return null;
+    :                     ^^^^^^^^^^^^
+ 51 |     const msg = this.queue[idx]!;
+    `----
+  help: Replace `return null;` with `{return null;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/message-queue.ts:88:40]
+ 87 |       // Unprocessed first
+ 88 |       if (a.processed !== b.processed) return a.processed ? 1 : -1;
+    :                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 89 |       // Then by priority
+    `----
+  help: Replace `return a.processed ? 1 : -1;` with `{return a.processed ? 1 : -1;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/message-queue.ts:92:22]
+ 91 |       const pb = PRIORITY_ORDER[b.priority] ?? 99;
+ 92 |       if (pa !== pb) return pa - pb;
+    :                      ^^^^^^^^^^^^^^^
+ 93 |       // Then by timestamp (oldest first)
+    `----
+  help: Replace `return pa - pb;` with `{return pa - pb;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/memory.ts:156:40]
+ 155 |       for (const kw of keywords) {
+ 156 |         if (patternLower.includes(kw)) score += 1;
+     :                                        ^^^^^^^^^^^
+ 157 |       }
+     `----
+  help: Replace `score += 1;` with `{score += 1;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/memory.ts:183:40]
+ 182 |         existing.notes.push(note);
+ 183 |         if (existing.notes.length > 5) existing.notes = existing.notes.slice(-5);
+     :                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 184 |       }
+     `----
+  help: Replace `existing.notes = existing.notes.slice(-5);` with `{existing.notes = existing.notes.slice(-5);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[website/js/main.js:60:27]
+ 59 |     const targetId = anchor.getAttribute("href");
+ 60 |     if (targetId === "#") return;
+    :                           ^^^^^^^
+ 61 |     const target = document.querySelector(targetId);
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/loop.ts:80:23]
+ 79 |   async run(): Promise<void> {
+ 80 |     if (this.running) return;
+    :                       ^^^^^^^
+ 81 |     this.running = true;
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/loop.ts:141:24]
+ 140 |     // If we were stopped by a CRITICAL message, bail out
+ 141 |     if (!this.running) return;
+     :                        ^^^^^^^
+ 142 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/loop.ts:172:28]
+ 171 |       for (const action of actions) {
+ 172 |         if (!this.running) return;
+     :                            ^^^^^^^
+ 173 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/loop.ts:177:30]
+ 176 |           await this.processUserMessages();
+ 177 |           if (!this.running) return;
+     :                              ^^^^^^^
+ 178 |         }
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/loop.ts:379:31]
+ 378 |   private setState(state: AgentState): void {
+ 379 |     if (this.state === state) return;
+     :                               ^^^^^^^
+ 380 |     this.state = state;
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/live-stream.ts:60:24]
+ 59 |   async start(controller: DeviceController): Promise<void> {
+ 60 |     if (this._running) return;
+    :                        ^^^^^^^
+ 61 |     this.controller = controller;
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/live-stream.ts:135:45]
+ 134 |   private async captureFrame(): Promise<void> {
+ 135 |     if (!this.controller || !this._running) return;
+     :                                             ^^^^^^^
+ 136 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/jarvis/tts-speaker.ts:61:39]
+ 60 |   async speak(text: string): Promise<void> {
+ 61 |     if (this.config.engine === "off") return;
+    :                                       ^^^^^^^
+ 62 |     if (!text.trim()) return;
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/jarvis/tts-speaker.ts:62:23]
+ 61 |     if (this.config.engine === "off") return;
+ 62 |     if (!text.trim()) return;
+    :                       ^^^^^^^
+ 63 | 
+    `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+    ,-[src/autonomous/jarvis/mic-listener.ts:68:22]
+ 67 |   const samples = pcm.length / 2; // 16-bit = 2 bytes per sample
+ 68 |   if (samples === 0) return 0;
+    :                      ^^^^^^^^^
+ 69 | 
+    `----
+  help: Replace `return 0;` with `{return 0;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/mic-listener.ts:112:23]
+ 111 |   async start(): Promise<void> {
+ 112 |     if (this.running) return;
+     :                       ^^^^^^^
+ 113 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/mic-listener.ts:176:24]
+ 175 |   stop(): void {
+ 176 |     if (!this.running) return;
+     :                        ^^^^^^^
+ 177 |     this.running = false;
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/jarvis-daemon.ts:167:31]
+ 166 |   async stop(): Promise<void> {
+ 167 |     if (this.state === "off") return;
+     :                               ^^^^^^^
+ 168 | 
+     `----
+  help: Replace `return;` with `{return;}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/jarvis-daemon.ts:204:22]
+ 203 |     this.config = { ...this.config, ...updates };
+ 204 |     if (updates.mic) this.mic.setConfig(updates.mic);
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 205 |     if (updates.stt) this.stt.setConfig(updates.stt);
+     `----
+  help: Replace `this.mic.setConfig(updates.mic);` with `{this.mic.setConfig(updates.mic);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/jarvis-daemon.ts:205:22]
+ 204 |     if (updates.mic) this.mic.setConfig(updates.mic);
+ 205 |     if (updates.stt) this.stt.setConfig(updates.stt);
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 206 |     if (updates.tts) this.tts.setConfig(updates.tts);
+     `----
+  help: Replace `this.stt.setConfig(updates.stt);` with `{this.stt.setConfig(updates.stt);}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[src/autonomous/jarvis/jarvis-daemon.ts:206:22]
+ 205 |     if (updates.stt) this.stt.setConfig(updates.stt);
+ 206 |     if (updates.tts) this.tts.setConfig(updates.tts);
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 207 |   }
+     `----
+  help: Replace `this.tts.setConfig(updates.tts);` with `{this.tts.setConfig(updates.tts);}`.
+
+  x eslint(no-unused-vars): Type 'JarvisConfig' is imported but never used.
+   ,-[src/autonomous/jarvis/jarvis-daemon.test.ts:2:52]
+ 1 | import { describe, it, expect, beforeEach } from "vitest";
+ 2 | import { JarvisDaemon, DEFAULT_JARVIS_CONFIG, type JarvisConfig } from "./jarvis-daemon.js";
+   :                                                    ^^^^^^|^^^^^
+   :                                                          `-- 'JarvisConfig' is imported here
+ 3 | 
+   `----
+  help: Consider removing this import.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/autonomous/jarvis/jarvis-daemon.test.ts:113:25]
+ 112 |       // Access private method for testing
+ 113 |       return (daemon as any).extractCommand(transcript);
+     :                         ^^^
+ 114 |     }
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x typescript-eslint(no-explicit-any): Unexpected `any`. Specify a different type.
+     ,-[src/cli/autonomous-cli.ts:146:17]
+ 145 |         : all.filter(
+ 146 |             (g: any) =>
+     :                 ^^^
+ 147 |               g.status === "pending" || g.status === "in-progress" || g.status === "paused",
+     `----
+  help: Use `unknown` instead, this will force you to explicitly, and safely, assert the type is correct.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[ui/src/ui/components/shared.ts:159:23]
+ 158 |     const classes = [];
+ 159 |     if (this.primary) classes.push("primary");
+     :                       ^^^^^^^^^^^^^^^^^^^^^^^^
+ 160 |     if (this.danger) classes.push("danger");
+     `----
+  help: Replace `classes.push("primary");` with `{classes.push("primary");}`.
+
+  x eslint(curly): Expected { after 'if' condition.
+     ,-[ui/src/ui/components/shared.ts:160:22]
+ 159 |     if (this.primary) classes.push("primary");
+ 160 |     if (this.danger) classes.push("danger");
+     :                      ^^^^^^^^^^^^^^^^^^^^^^^
+ 161 |     return html`<button class=${classes.join(" ")} ?disabled=${this.disabled}><slot></slot></button>`;
+     `----
+  help: Replace `classes.push("danger");` with `{classes.push("danger");}`.
+
+  x eslint(no-unused-vars): Identifier 'formatNextRun' is imported but never used.
+   ,-[ui/src/ui/views/overview.ts:5:10]
+ 4 | import { formatRelativeTimestamp, formatDurationHuman } from "../format.ts";
+ 5 | import { formatNextRun } from "../presenter.ts";
+   :          ^^^^^^|^^^^^^
+   :                `-- 'formatNextRun' is imported here
+ 6 | 
+   `----
+  help: Consider removing this import.
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/self-analyze.ts:276:28]
+ 275 |     );
+ 276 |     const first = new Date(sorted[0]!.timestamp).getTime();
+     :                            ^^^^^^^^^^
+ 277 |     const last = new Date(sorted[sorted.length - 1]!.timestamp).getTime();
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/self-analyze.ts:277:27]
+ 276 |     const first = new Date(sorted[0]!.timestamp).getTime();
+ 277 |     const last = new Date(sorted[sorted.length - 1]!.timestamp).getTime();
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 278 |     durations.set(goalId, last - first);
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/auth-manager.ts:60:19]
+ 59 |         secure: c.secure,
+ 60 |         sameSite: c.sameSite as CookieData["sameSite"],
+    :                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 61 |       }));
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/message-queue.ts:51:17]
+ 50 |     if (idx === -1) return null;
+ 51 |     const msg = this.queue[idx]!;
+    :                 ^^^^^^^^^^^^^^^^
+ 52 |     msg.processed = true;
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/self-analyze.ts:330:26]
+ 329 |       if (strategyMatch) {
+ 330 |         const strategy = strategyMatch[1]!.toLowerCase();
+     :                          ^^^^^^^^^^^^^^^^^
+ 331 |         if (!strategyData.has(strategy)) {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/self-improve.ts:297:76]
+ 296 |       if (insight.includes("Extract procedures") || insight.includes("best performing")) {
+ 297 |         const existing = this.state.promptAdditions.find((p) => p.includes(insight.split(":")[0]!));
+     :                                                                            ^^^^^^^^^^^^^^^^^^^^^^
+ 298 |         if (!existing) {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/self-improve.ts:350:21]
+ 349 |       }
+ 350 |       const hints = this.state.domainHints[profile.domain]!;
+     :                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 351 | 
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/device/linux-backend.ts:346:34]
+ 345 |       if (match) {
+ 346 |         return { width: parseInt(match[1]!, 10), height: parseInt(match[2]!, 10) };
+     :                                  ^^^^^^^^^
+ 347 |       }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/device/linux-backend.ts:346:67]
+ 345 |       if (match) {
+ 346 |         return { width: parseInt(match[1]!, 10), height: parseInt(match[2]!, 10) };
+     :                                                                   ^^^^^^^^^
+ 347 |       }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/device-actions.ts:176:50]
+ 175 |     default: {
+ 176 |       throw new Error(`Unknown device action: ${(action as DeviceAction).action}`);
+     :                                                  ^^^^^^^^^^^^^^^^^^^^^^
+ 177 |     }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/playbooks.ts:204:65]
+ 203 |     } catch (err) {
+ 204 |       this.emit("recon", "failed", `Playbook failed abruptly: ${err}`);
+     :                                                                 ^^^
+ 205 |       throw err;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:162:22]
+ 161 |       for (let i = 0; i < plan.steps.length; i++) {
+ 162 |         const step = plan.steps[i]!;
+     :                      ^^^^^^^^^^^^^^
+ 163 | 
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:232:23]
+ 231 |     if (openAppMatch) {
+ 232 |       const appName = openAppMatch[1]!.trim();
+     :                       ^^^^^^^^^^^^^^^^
+ 233 |       return {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:279:21]
+ 278 |     if (searchMatch) {
+ 279 |       const query = searchMatch[1]!;
+     :                     ^^^^^^^^^^^^^^^
+ 280 |       return {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:305:23]
+ 304 |     if (imageMatch) {
+ 305 |       const imgPath = imageMatch[1]!.trim();
+     :                       ^^^^^^^^^^^^^^
+ 306 |       return {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:364:23]
+ 363 |             action: {
+ 364 |               action: actionMap[action!]! as any,
+     :                       ^^^^^^^^^^^^^^^^^^^
+ 365 |               windowTitle: target?.trim(),
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:364:33]
+ 363 |             action: {
+ 364 |               action: actionMap[action!]! as any,
+     :                                 ^^^^^^^
+ 365 |               windowTitle: target?.trim(),
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:408:19]
+ 407 |     if (hotkeyMatch) {
+ 408 |       const mod = hotkeyMatch[1]!.toLowerCase().replace("cmd", "meta").replace("super", "meta");
+     :                   ^^^^^^^^^^^^^^^
+ 409 |       const key = hotkeyMatch[2]!.trim().toLowerCase();
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:409:19]
+ 408 |       const mod = hotkeyMatch[1]!.toLowerCase().replace("cmd", "meta").replace("super", "meta");
+ 409 |       const key = hotkeyMatch[2]!.trim().toLowerCase();
+     :                   ^^^^^^^^^^^^^^^
+ 410 |       return {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/voice.ts:169:12]
+ 168 |     ];
+ 169 |     return replies[Math.floor(Math.random() * replies.length)]!;
+     :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 170 |   }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:464:28]
+ 463 |               action: "device_focus_window",
+ 464 |               windowTitle: switchMatch[1]!.trim(),
+     :                            ^^^^^^^^^^^^^^^
+ 465 |               confidence: 0.85,
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/nl-commander.ts:480:22]
+ 479 |     if (pentestMatch) {
+ 480 |       const target = pentestMatch[1]!.trim();
+     :                      ^^^^^^^^^^^^^^^^
+ 481 | 
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/nl-commander.ts:497:44]
+ 496 |         } catch (err) {
+ 497 |           log.error(`AutoPentest failed: ${err}`);
+     :                                            ^^^
+ 498 |         }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "never" of template literal expression.
+    ,-[src/autonomous/jarvis/stt-engine.ts:91:48]
+ 90 |       default:
+ 91 |         throw new Error(`Unknown STT engine: ${this.config.engine}`);
+    :                                                ^^^^^^^^^^^^^^^^^^
+ 92 |     }
+    `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/jarvis/jarvis-daemon.ts:131:65]
+ 130 |     } catch (err) {
+ 131 |       log.warn(`Failed to load wake triggers, using defaults: ${err}`);
+     :                                                                 ^^^
+ 132 |     }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/scheduler.ts:81:29]
+ 80 |   return {
+ 81 |     minutes: parseCronField(parts[0]!, 0, 59),
+    :                             ^^^^^^^^^
+ 82 |     hours: parseCronField(parts[1]!, 0, 23),
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/scheduler.ts:82:27]
+ 81 |     minutes: parseCronField(parts[0]!, 0, 59),
+ 82 |     hours: parseCronField(parts[1]!, 0, 23),
+    :                           ^^^^^^^^^
+ 83 |     daysOfMonth: parseCronField(parts[2]!, 1, 31),
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/scheduler.ts:83:33]
+ 82 |     hours: parseCronField(parts[1]!, 0, 23),
+ 83 |     daysOfMonth: parseCronField(parts[2]!, 1, 31),
+    :                                 ^^^^^^^^^
+ 84 |     months: parseCronField(parts[3]!, 1, 12),
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/scheduler.ts:84:28]
+ 83 |     daysOfMonth: parseCronField(parts[2]!, 1, 31),
+ 84 |     months: parseCronField(parts[3]!, 1, 12),
+    :                            ^^^^^^^^^
+ 85 |     daysOfWeek: parseCronField(parts[4]!, 0, 6),
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/scheduler.ts:85:32]
+ 84 |     months: parseCronField(parts[3]!, 1, 12),
+ 85 |     daysOfWeek: parseCronField(parts[4]!, 0, 6),
+    :                                ^^^^^^^^^
+ 86 |   };
+    `----
+
+  x typescript-eslint(no-base-to-string): 'params.method ?? "GET"' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/plugins.ts:250:33]
+ 249 |           const url = String(params.url);
+ 250 |           const method = String(params.method ?? "GET").toUpperCase();
+     :                                 ^^^^^^^^^^^^^^^^^^^^^^
+ 251 |           const headers: Record<string, string> = {
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'params.body' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/plugins.ts:260:42]
+ 259 |               headers,
+ 260 |               body: params.body ? String(params.body) : undefined,
+     :                                          ^^^^^^^^^^^
+ 261 |             });
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/plugins.ts:338:59]
+ 337 |             await wf(filePath, String(params.content));
+ 338 |             return { success: true, output: `Written to ${params.path}` };
+     :                                                           ^^^^^^^^^^^
+ 339 |           } catch (err) {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/tab-manager.ts:106:30]
+ 105 |       if (remaining.length > 0) {
+ 106 |         return this.switchTo(remaining[0]!.id);
+     :                              ^^^^^^^^^^^^^
+ 107 |       }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/visual-actions.ts:114:48]
+ 113 |     default: {
+ 114 |       throw new Error(`Unknown action type: ${(action as Action).action}`);
+     :                                                ^^^^^^^^^^^^^^^^
+ 115 |     }
+     `----
+
+  x typescript-eslint(no-base-to-string): 'suggestedAction?.reasoning ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:228:25]
+ 227 |       confidence: Number(suggestedAction?.confidence ?? 0.5),
+ 228 |       reasoning: String(suggestedAction?.reasoning ?? ""),
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 229 |     };
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'suggestedAction.text' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:235:53]
+ 234 |     }
+ 235 |     if (suggestedAction?.text) action.text = String(suggestedAction.text);
+     :                                                     ^^^^^^^^^^^^^^^^^^^^
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'suggestedAction.url' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:236:51]
+ 235 |     if (suggestedAction?.text) action.text = String(suggestedAction.text);
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+     :                                                   ^^^^^^^^^^^^^^^^^^^
+ 237 |     if (suggestedAction?.key) action.key = String(suggestedAction.key);
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'suggestedAction.key' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:237:51]
+ 236 |     if (suggestedAction?.url) action.url = String(suggestedAction.url);
+ 237 |     if (suggestedAction?.key) action.key = String(suggestedAction.key);
+     :                                                   ^^^^^^^^^^^^^^^^^^^
+ 238 |     if (suggestedAction?.direction) action.direction = suggestedAction.direction as "up" | "down";
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'parsed.observation ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:242:27]
+ 241 |     return {
+ 242 |       observation: String(parsed.observation ?? ""),
+     :                           ^^^^^^^^^^^^^^^^^^^^^^^^
+ 243 |       reasoning: String(parsed.reasoning ?? ""),
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'parsed.reasoning ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/vision.ts:243:25]
+ 242 |       observation: String(parsed.observation ?? ""),
+ 243 |       reasoning: String(parsed.reasoning ?? ""),
+     :                         ^^^^^^^^^^^^^^^^^^^^^^
+ 244 |       suggestedAction: action,
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/daemon.ts:204:15]
+ 203 |       const dashUrl = await this.dashboard.start({
+ 204 |         page: this.page!,
+     :               ^^^^^^^^^^
+ 205 |         agent: this.agent,
+     `----
+
+  x typescript-eslint(no-base-to-string): 'data' may use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:102:34]
+ 101 |         try {
+ 102 |           const msg = JSON.parse(data.toString());
+     :                                  ^^^^
+ 103 |           void this.handleClientMessage(msg, ws);
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'msg.description ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:209:36]
+ 208 |       case "add_goal": {
+ 209 |         const description = String(msg.description ?? "");
+     :                                    ^^^^^^^^^^^^^^^^^^^^^
+ 210 |         const priority = (msg.priority as Goal["priority"]) ?? "normal";
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'msg.content ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:219:32]
+ 218 |       case "user_message": {
+ 219 |         const content = String(msg.content ?? "");
+     :                                ^^^^^^^^^^^^^^^^^
+ 220 |         if (content && this.ui) {
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'msg.goalId ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:228:31]
+ 227 |       case "pause_goal": {
+ 228 |         const goalId = String(msg.goalId ?? "");
+     :                               ^^^^^^^^^^^^^^^^
+ 229 |         if (goalId && this.agent) {
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'msg.goalId ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:237:31]
+ 236 |       case "cancel_goal": {
+ 237 |         const goalId = String(msg.goalId ?? "");
+     :                               ^^^^^^^^^^^^^^^^
+ 238 |         if (goalId && this.agent) {
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-base-to-string): 'msg.url ?? ""' will use Object's default stringification format ('[object Object]') when stringified.
+     ,-[src/autonomous/browser-ui/server.ts:267:31]
+ 266 |       case "navigate": {
+ 267 |         const navUrl = String(msg.url ?? "");
+     :                               ^^^^^^^^^^^^^
+ 268 |         if (navUrl && this.page) {
+     `----
+  help: Consider picking a property (e.g. `user.name`), using a formatter (or `JSON.stringify`), or implementing a custom `toString()`/`toLocaleString()` on the type.
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/user-profile.ts:144:17]
+ 143 |     }
+ 144 |     const app = this.data.apps[key]!;
+     :                 ^^^^^^^^^^^^^^^^^^^^
+ 145 |     app.launchCount++;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/workflow-replay.ts:111:11]
+ 110 |       this.currentRecording.length > 0
+ 111 |         ? this.currentRecording[this.currentRecording.length - 1]!.timestamp
+     :           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 112 |         : 0;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/user-profile.ts:162:23]
+ 161 |       .filter((app) => app.timeSlots[hour] > 0)
+ 162 |       .sort((a, b) => b.timeSlots[hour]! - a.timeSlots[hour]!)
+     :                       ^^^^^^^^^^^^^^^^^^
+ 163 |       .slice(0, 5);
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/user-profile.ts:162:44]
+ 161 |       .filter((app) => app.timeSlots[hour] > 0)
+ 162 |       .sort((a, b) => b.timeSlots[hour]! - a.timeSlots[hour]!)
+     :                                            ^^^^^^^^^^^^^^^^^^
+ 163 |       .slice(0, 5);
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/workflow-replay.ts:130:11]
+ 129 |       this.currentRecording.length > 0
+ 130 |         ? this.currentRecording[this.currentRecording.length - 1]!.timestamp
+     :           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 131 |         : 0;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/autonomous/device/android-backend.ts:92:21]
+ 91 |     if (!this.serial) {
+ 92 |       this.serial = devices[0]!.serial;
+    :                     ^^^^^^^^^^^
+ 93 |     }
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/device/ios-backend.ts:138:25]
+ 137 | 
+ 138 |     this.udid = udid ?? devices[0]!.udid;
+     :                         ^^^^^^^^^^^
+ 139 |     this.wdaBaseUrl = `http://localhost:${wdaPort}`;
+     `----
+
+  x typescript-eslint(no-unnecessary-boolean-literal-compare): This expression unnecessarily compares a boolean value to a boolean instead of using it directly.
+     ,-[src/autonomous/device/ios-backend.ts:566:33]
+ 565 |     // Minimum requirement: either libimobiledevice OR tidevice
+ 566 |     const hasLibimobiledevice = tools["idevice_id"] === true;
+     :                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 567 |     const hasTidevice = tools["tidevice"] === true;
+     `----
+
+  x typescript-eslint(no-unnecessary-boolean-literal-compare): This expression unnecessarily compares a boolean value to a boolean instead of using it directly.
+     ,-[src/autonomous/device/ios-backend.ts:567:25]
+ 566 |     const hasLibimobiledevice = tools["idevice_id"] === true;
+ 567 |     const hasTidevice = tools["tidevice"] === true;
+     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 568 |     const available = hasLibimobiledevice || hasTidevice;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/screen-ocr.ts:213:29]
+ 212 |         if (slice.join(" ").includes(searchLower)) {
+ 213 |           const firstWord = result.words[i]!;
+     :                             ^^^^^^^^^^^^^^^^
+ 214 |           const lastWord = result.words[i + searchWords.length - 1]!;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/screen-ocr.ts:214:28]
+ 213 |           const firstWord = result.words[i]!;
+ 214 |           const lastWord = result.words[i + searchWords.length - 1]!;
+     :                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 215 |           const x = firstWord.bbox.x;
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/multi-monitor.ts:146:17]
+ 145 |         currentMonitor = {
+ 146 |           name: monitorMatch[1]!,
+     :                 ^^^^^^^^^^^^^^^^
+ 147 |           connected: monitorMatch[2] === "connected",
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/multi-monitor.ts:163:41]
+ 162 |         currentMonitor.resolutions = currentMonitor.resolutions ?? [];
+ 163 |         currentMonitor.resolutions.push(resMatch[1]!);
+     :                                         ^^^^^^^^^^^^
+ 164 |         if (resMatch[3]?.includes("*")) {
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/multi-monitor.ts:165:51]
+ 164 |         if (resMatch[3]?.includes("*")) {
+ 165 |           currentMonitor.refreshRate = parseFloat(resMatch[2]!);
+     :                                                   ^^^^^^^^^^^^
+ 166 |         }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[src/autonomous/ai-delegation.ts:197:23]
+ 196 |       while (index < agents.length) {
+ 197 |         const agent = agents[index]!;
+     :                       ^^^^^^^^^^^^^^
+ 198 |         index++;
+     `----
+
+  x typescript-eslint(await-thenable): Unexpected `await` of a non-Promise (non-"Thenable") value.
+     ,-[src/autonomous/security/web-scanner.ts:347:24]
+ 346 |           const response = await this.fetch(testUrl.toString(), options);
+ 347 |           const body = await response;
+     :                        ^^^^^^^^^^^^^^
+ 348 | 
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/wifi-security.ts:135:59]
+ 134 |     } catch (e) {
+ 135 |       this.emit({ kind: "error", message: `Scan failed: ${e}` });
+     :                                                           ^
+ 136 |     }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/crypto-toolkit.ts:164:24]
+ 163 |     } catch (e) {
+ 164 |       return `Error: ${e}`;
+     :                        ^
+ 165 |     }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/wifi-security.ts:184:61]
+ 183 |     } catch (e) {
+ 184 |       this.emit({ kind: "error", message: `Deauth failed: ${e}` });
+     :                                                             ^
+ 185 |     }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/exploit-engine.ts:122:62]
+ 121 |       const result: ExploitResult = { success: false, target, exploit: module, output: String(e) };
+ 122 |       this.emit({ kind: "error", message: `Exploit failed: ${e}` });
+     :                                                              ^
+ 123 |       return result;
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/exploit-engine.ts:178:24]
+ 177 |     } catch (e) {
+ 178 |       return `Error: ${e}`;
+     :                        ^
+ 179 |     }
+     `----
+
+  x typescript-eslint(restrict-template-expressions): Invalid type "unknown" of template literal expression.
+     ,-[src/autonomous/security/exploit-engine.ts:187:24]
+ 186 |     } catch (e) {
+ 187 |       return `Error: ${e}`;
+     :                        ^
+ 188 |     }
+     `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+    ,-[src/gateway/server-methods/jarvis.ts:86:24]
+ 85 |       // SET: update config
+ 86 |       jarvis.setConfig(params as Record<string, unknown>);
+    :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
+ 87 |       respond(true, { message: "Config updated", config: jarvis.getConfig() });
+    `----
+
+  x typescript-eslint(no-unnecessary-type-assertion): This assertion is unnecessary since it does not change the type of the expression.
+     ,-[ui/src/ui/app.ts:633:19]
+ 632 |         try {
+ 633 | ,->       const res = (await this.client.request("agents.spawn", { id: agentId })) as {
+ 634 | |           session?: string;
+ 635 | `->       };
+ 636 |           if (res.session) {
+     `----
+
+  x typescript-eslint(no-floating-promises): Promises must be awaited, add void operator to ignore.
+   ,-[src/autonomous/security/test_pentest.ts:7:1]
+ 6 | }
+ 7 | test();
+   : ^^^^^^^
+   `----
+  help: The promise must end with a call to .catch, or end with a call to .then with a rejection handler, or be explicitly marked as ignored with the `void` operator.
+
+  x typescript-eslint(no-floating-promises): Promises must be awaited, add void operator to ignore.
+    ,-[src/autonomous/security/test_pentest_live.ts:12:1]
+ 11 | }
+ 12 | test();
+    : ^^^^^^^
+    `----
+  help: The promise must end with a call to .catch, or end with a call to .then with a rejection handler, or be explicitly marked as ignored with the `void` operator.
+
+Found 0 warnings and 462 errors.
+Finished in 11.2s on 3252 files with 136 rules using 16 threads.
+ΓÇëELIFECYCLEΓÇë Command failed with exit code 1.
diff --git a/lint-error.txt b/lint-error.txt
new file mode 100644
index 000000000..be6de523c
Binary files /dev/null and b/lint-error.txt differ
diff --git a/ui/src/ui/app-view-state.ts b/ui/src/ui/app-view-state.ts
index 47f9ff233..1233fe0ba 100644
--- a/ui/src/ui/app-view-state.ts
+++ b/ui/src/ui/app-view-state.ts
@@ -287,4 +287,5 @@ export type AppViewState = {
   handleOpenSidebar: (content: string) => void;
   handleCloseSidebar: () => void;
   handleSplitRatioChange: (ratio: number) => void;
+  handleSpawnAgent: (agentId: string) => Promise<void>;
 };
diff --git a/ui/src/ui/app.ts b/ui/src/ui/app.ts
index e295afd15..e4e208bcf 100644
--- a/ui/src/ui/app.ts
+++ b/ui/src/ui/app.ts
@@ -624,6 +624,24 @@ export class AgdiApp extends LitElement {
     }
   }
 
+  async handleSpawnAgent(agentId: string) {
+    if (!this.client?.connected) {
+      this.lastError = "Cannot spawn agent: Not connected to gateway.";
+      return;
+    }
+    try {
+      const res = await this.client.request("agents.spawn", { id: agentId });
+      const session =
+        res && typeof res === "object" ? (res as Record<string, unknown>).session : null;
+      if (typeof session === "string") {
+        this.sessionKey = session;
+        this.setTab("chat");
+      }
+    } catch (err: unknown) {
+      this.lastError = `Failed to spawn agent: ${err instanceof Error ? err.message : String(err)}`;
+    }
+  }
+
   render() {
     return renderApp(this as unknown as AppViewState);
   }
diff --git a/ui/src/ui/views/agents.ts b/ui/src/ui/views/agents.ts
index f8cf5cb5f..fccedd623 100644
--- a/ui/src/ui/views/agents.ts
+++ b/ui/src/ui/views/agents.ts
@@ -77,6 +77,7 @@ export type AgentsProps = {
   onConfigSave: () => void;
   onModelChange: (agentId: string, modelId: string | null) => void;
   onModelFallbacksChange: (agentId: string, fallbacks: string[]) => void;
+  onSpawnAgent?: (agentId: string) => void;
   onChannelsRefresh: () => void;
   onCronRefresh: () => void;
   onSkillsFilterChange: (next: string) => void;
@@ -180,6 +181,7 @@ export function renderAgents(props: AgentsProps) {
                         onConfigSave: props.onConfigSave,
                         onModelChange: props.onModelChange,
                         onModelFallbacksChange: props.onModelFallbacksChange,
+                        onSpawnAgent: props.onSpawnAgent,
                       })
                     : nothing
                 }
@@ -352,6 +354,7 @@ function renderAgentOverview(params: {
   onConfigSave: () => void;
   onModelChange: (agentId: string, modelId: string | null) => void;
   onModelFallbacksChange: (agentId: string, fallbacks: string[]) => void;
+  onSpawnAgent?: (agentId: string) => void;
 }) {
   const {
     agent,
@@ -367,6 +370,7 @@ function renderAgentOverview(params: {
     onConfigSave,
     onModelChange,
     onModelFallbacksChange,
+    onSpawnAgent,
   } = params;
   const config = resolveAgentConfig(configForm, agent.id);
   const workspaceFromFiles =
@@ -471,7 +475,17 @@ function renderAgentOverview(params: {
             />
           </label>
         </div>
-        <div class="row" style="justify-content: flex-end; gap: 8px;">
+        <div class="row" style="justify-content: flex-end; gap: 8px; margin-top: 16px; align-items: center; border-top: 1px solid var(--border); padding-top: 16px;">
+          <div style="flex: 1;">
+            <button
+              class="btn btn--sm"
+              ?disabled=${!onSpawnAgent}
+              @click=${() => onSpawnAgent?.(agent.id)}
+              title="Spawn a new child session of this agent"
+            >
+              Spawn Sub-Agent
+            </button>
+          </div>
           <button class="btn btn--sm" ?disabled=${configLoading} @click=${onConfigReload}>
             Reload Config
           </button>
diff --git a/ui/src/ui/views/chat.ts b/ui/src/ui/views/chat.ts
index a74b6cf16..0398a93bb 100644
--- a/ui/src/ui/views/chat.ts
+++ b/ui/src/ui/views/chat.ts
@@ -370,6 +370,44 @@ export function renderChat(props: ChatProps) {
 
       <div class="chat-compose">
         ${renderAttachmentPreview(props)}
+        <div class="chat-quick-actions" style="display: flex; gap: 8px; margin-bottom: 8px; flex-wrap: wrap; padding: 0 12px;">
+          <button
+            class="btn btn--sm"
+            type="button"
+            title="Open a new canvas artifact"
+            ?disabled=${!props.connected}
+            @click=${() => {
+              const text = "Open a new canvas artifact for ";
+              props.onDraftChange((props.draft || "") + text);
+            }}
+          >
+            ${icons.fileText} Canvas
+          </button>
+          <button
+            class="btn btn--sm"
+            type="button"
+            title="Run a command in the sandbox"
+            ?disabled=${!props.connected}
+            @click=${() => {
+              const text = "Run the following shell command:\n```bash\n\n```";
+              props.onDraftChange((props.draft || "") + text);
+            }}
+          >
+            ${icons.zap} Run Command
+          </button>
+          <button
+            class="btn btn--sm"
+            type="button"
+            title="Navigate to a URL"
+            ?disabled=${!props.connected}
+            @click=${() => {
+              const text = "Navigate to the following URL in the browser: ";
+              props.onDraftChange((props.draft || "") + text);
+            }}
+          >
+            ${icons.globe} Browse
+          </button>
+        </div>
         <div class="chat-compose__row">
           <label class="field chat-compose__field">
             <span>Message</span>
